---
phase: 26-persona-entrevistador-forgeclaw
plan: 26-04
type: test
autonomous: true
wave: 2
depends_on: ["26-01", "26-02", "26-03"]
requirements: [INT-09, INT-10]
files_modified:
  - packages/core/tests/onboarding/prompts.test.ts
  - packages/core/tests/onboarding/interviewer.test.ts
  - packages/core/tests/onboarding/fixtures/content-creator-happy.json
  - packages/core/tests/onboarding/fixtures/solo-builder-budget-exceeded.json
  - packages/core/tests/onboarding/fixtures/generic-abort.json
  - packages/core/tests/onboarding/fixtures/malformed-response.json
must_haves:
  truths:
    - "Testes unitarios cobrem extractJsonBlock, validateInterviewResponse, validateHarnessDiff, loadInterviewerPrompt, loadScript"
    - "Teste de integracao da classe Interviewer usa MOCK do ClaudeRunner (fixtures de NDJSON) — nao spawna claude CLI de verdade"
    - "4 fixtures cobrem: happy path (asking -> asking -> done), budget cutoff (max_turns), user abort, response parse error"
    - "Teste valida que aplicar o diff final resultante do happy path produz mudancas concretas em arquivos de harness temporarios"
    - "Suite roda em CI com `vitest run packages/core/tests/onboarding/` verde"
  artifacts:
    - path: "packages/core/tests/onboarding/prompts.test.ts"
      provides: "Testes unitarios puros do loader + validators"
    - path: "packages/core/tests/onboarding/interviewer.test.ts"
      provides: "Teste de integracao com mock de ClaudeRunner"
    - path: "packages/core/tests/onboarding/fixtures/*.json"
      provides: "Fixtures de NDJSON que simulam output do claude CLI"
  key_links:
    - from: "interviewer.test.ts"
      to: "fixtures/*.json"
      via: "readFileSync + substituicao do spawn via vi.mock de bun/spawn"
    - from: "interviewer.test.ts"
      to: "packages/core/src/onboarding/interviewer.ts"
      via: "import { Interviewer } + vi.mock do claude-runner"
---

# Fase 26 Plano 04: Testes do Entrevistador (unitario + integracao com fixtures)

**Objetivo:** Provar que o entrevistador funciona end-to-end sem depender do claude CLI real. Usa mock do ClaudeRunner alimentado por fixtures de NDJSON que simulam respostas do modelo. Cobre happy path, cutoffs de budget, abort e parsing de resposta malformada.

## Contexto

@packages/core/src/onboarding/types.ts — contratos
@packages/core/src/onboarding/prompts.ts — funcoes a testar (extractJsonBlock, validators)
@packages/core/src/onboarding/interviewer.ts — classe a testar
@packages/core/src/onboarding/interviewer.md — system prompt (nao modificar, apenas ler)
@packages/core/tests/event-bus.test.ts — referencia de estilo vitest
@packages/core/src/claude-runner.ts — forma do AsyncGenerator<StreamEvent> que sera mockado

**Nota crítica:** `packages/core` usa `vitest`, nao `bun:test` (ver imports em `event-bus.test.ts`). Use vitest consistentemente.

## Estrategia de mock

A classe `Interviewer` importa `ClaudeRunner` de `../claude-runner`. Para teste:

1. Usar `vi.mock('../../src/claude-runner', ...)` no topo do arquivo de teste.
2. O mock expoe uma classe `ClaudeRunner` com metodo `run(prompt, opts)` que retorna um `AsyncGenerator<StreamEvent>` controlado pelo teste.
3. Cada fixture define uma lista ordenada de `StreamEvent`s (tipo `text`, `done` com usage etc.).
4. O teste carrega a fixture, configura o mock pra retornar os eventos na ordem e roda `Interviewer.start()` + `Interviewer.answer(...)`.

## Tarefas

<task id="1" type="auto">
<files>packages/core/tests/onboarding/prompts.test.ts</files>
<action>
Criar testes unitarios para `extractJsonBlock`, `validateInterviewResponse`, `validateHarnessDiff`, `validateDiffOp`, `loadInterviewerPrompt`, `loadScript`.

Criar `packages/core/tests/onboarding/prompts.test.ts` com conteudo EXATO:

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import {
  extractJsonBlock,
  validateInterviewResponse,
  validateHarnessDiff,
  validateDiffOp,
  loadInterviewerPrompt,
  loadInterviewerBase,
  loadScript,
  VALID_PLACEHOLDER_KEYS,
  _resetPromptCache,
} from '../../src/onboarding/prompts';
import {
  InterviewResponseParseError,
  InterviewDiffValidationError,
  type ArchetypeSlug,
} from '../../src/onboarding/types';

describe('extractJsonBlock', () => {
  it('extracts JSON from ```json fenced block```', () => {
    const raw = 'some preamble\n```json\n{ "status": "asking", "nextQuestion": "q?" }\n```\ntail';
    const out = extractJsonBlock(raw) as { status?: string };
    expect(out?.status).toBe('asking');
  });

  it('returns null when block present but invalid JSON', () => {
    const raw = '```json\n{ invalid\n```';
    expect(extractJsonBlock(raw)).toBeNull();
  });

  it('falls back to parsing entire raw when no fenced block', () => {
    const raw = '{"status":"aborted","reason":"x"}';
    const out = extractJsonBlock(raw) as { status?: string };
    expect(out?.status).toBe('aborted');
  });

  it('returns null when no JSON at all', () => {
    expect(extractJsonBlock('hello world')).toBeNull();
  });

  it('extracts FIRST block when multiple exist', () => {
    const raw = '```json\n{"status":"asking","nextQuestion":"first"}\n```\nand then\n```json\n{"status":"aborted","reason":"second"}\n```';
    const out = extractJsonBlock(raw) as { status?: string; nextQuestion?: string };
    expect(out?.status).toBe('asking');
    expect(out?.nextQuestion).toBe('first');
  });
});

describe('validateInterviewResponse', () => {
  it('accepts valid asking', () => {
    const r = validateInterviewResponse({
      status: 'asking',
      nextQuestion: 'Qual seu nome?',
      rationale: 'basico',
    });
    expect(r.status).toBe('asking');
    if (r.status === 'asking') {
      expect(r.nextQuestion).toBe('Qual seu nome?');
    }
  });

  it('accepts valid done with diff', () => {
    const r = validateInterviewResponse({
      status: 'done',
      summary: 'tudo ok',
      harnessDiff: {
        summary: 'tudo ok',
        diffs: [
          {
            file: 'USER.md',
            ops: [{ op: 'set_placeholder', key: 'userName', value: 'Ana' }],
          },
        ],
      },
    });
    expect(r.status).toBe('done');
  });

  it('accepts valid aborted', () => {
    const r = validateInterviewResponse({ status: 'aborted', reason: 'x' });
    expect(r.status).toBe('aborted');
  });

  it('throws on missing status', () => {
    expect(() => validateInterviewResponse({})).toThrow(InterviewResponseParseError);
  });

  it('throws on unknown status', () => {
    expect(() => validateInterviewResponse({ status: 'weird' })).toThrow(InterviewResponseParseError);
  });

  it('throws on asking without nextQuestion', () => {
    expect(() => validateInterviewResponse({ status: 'asking' })).toThrow(InterviewResponseParseError);
  });

  it('throws on done without summary', () => {
    expect(() =>
      validateInterviewResponse({
        status: 'done',
        harnessDiff: { summary: 'x', diffs: [] },
      }),
    ).toThrow(InterviewResponseParseError);
  });

  it('throws on aborted without reason', () => {
    expect(() => validateInterviewResponse({ status: 'aborted' })).toThrow(InterviewResponseParseError);
  });
});

describe('validateHarnessDiff', () => {
  it('accepts empty diffs array', () => {
    const r = validateHarnessDiff({ summary: 'nothing', diffs: [] });
    expect(r.diffs).toHaveLength(0);
  });

  it('throws on missing summary', () => {
    expect(() => validateHarnessDiff({ diffs: [] })).toThrow(InterviewDiffValidationError);
  });

  it('throws on invalid file name', () => {
    expect(() =>
      validateHarnessDiff({
        summary: 's',
        diffs: [{ file: 'FOO.md', ops: [] }],
      }),
    ).toThrow(InterviewDiffValidationError);
  });

  it('throws on non-array ops', () => {
    expect(() =>
      validateHarnessDiff({
        summary: 's',
        diffs: [{ file: 'USER.md', ops: 'bogus' }],
      }),
    ).toThrow(InterviewDiffValidationError);
  });
});

describe('validateDiffOp', () => {
  it('accepts append', () => {
    const op = validateDiffOp({ op: 'append', content: 'x' });
    expect(op.op).toBe('append');
  });

  it('accepts replace', () => {
    const op = validateDiffOp({ op: 'replace', find: 'a', replace: 'b' });
    expect(op.op).toBe('replace');
  });

  it('accepts replace_section with H2 header', () => {
    const op = validateDiffOp({
      op: 'replace_section',
      header: '## Intro',
      content: 'x',
      createIfMissing: true,
    });
    expect(op.op).toBe('replace_section');
  });

  it('throws on replace_section with H3 header', () => {
    expect(() =>
      validateDiffOp({ op: 'replace_section', header: '### sub', content: 'x' }),
    ).toThrow(InterviewDiffValidationError);
  });

  it('accepts set_placeholder with valid key', () => {
    for (const key of VALID_PLACEHOLDER_KEYS) {
      const op = validateDiffOp({ op: 'set_placeholder', key, value: 'x' });
      expect(op.op).toBe('set_placeholder');
    }
  });

  it('throws on set_placeholder with invalid key', () => {
    expect(() =>
      validateDiffOp({ op: 'set_placeholder', key: 'today', value: 'x' }),
    ).toThrow(InterviewDiffValidationError);
    expect(() =>
      validateDiffOp({ op: 'set_placeholder', key: 'notARealKey', value: 'x' }),
    ).toThrow(InterviewDiffValidationError);
  });

  it('throws on unknown op', () => {
    expect(() => validateDiffOp({ op: 'wipe_file' })).toThrow(InterviewDiffValidationError);
  });
});

describe('loadInterviewerPrompt', () => {
  beforeEach(() => _resetPromptCache());

  it('loads base prompt without crashing', () => {
    const base = loadInterviewerBase();
    expect(base).toContain('ForgeClaw Interviewer');
    expect(base.length).toBeGreaterThan(500);
  });

  const slugs: ArchetypeSlug[] = [
    'solo-builder',
    'content-creator',
    'agency-freela',
    'ecom-manager',
    'generic',
  ];

  it.each(slugs)('loadInterviewerPrompt(%s) concatenates base + script', (slug) => {
    const prompt = loadInterviewerPrompt(slug);
    expect(prompt).toContain('ForgeClaw Interviewer');
    // Cada script tem um heading Roteiro
    expect(prompt).toMatch(/Roteiro:/);
    // Separador presente
    expect(prompt).toContain('\n\n---\n\n');
  });

  it.each(slugs)('loadScript(%s) returns non-empty string', (slug) => {
    const s = loadScript(slug);
    expect(s).not.toBeNull();
    expect((s ?? '').length).toBeGreaterThan(100);
  });
});
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && test -f packages/core/tests/onboarding/prompts.test.ts && cd packages/core && (bun x vitest run tests/onboarding/prompts.test.ts --reporter=basic 2>&1 || npx vitest run tests/onboarding/prompts.test.ts --reporter=basic 2>&1) | tee /tmp/vitest-26-04-p.log | tail -20 && ! grep -iE "^\s*(failed|failures:)\s+[1-9]" /tmp/vitest-26-04-p.log</automated></verify>
<done>Testes de prompts.ts rodam verdes: extractJsonBlock, validateInterviewResponse, validateHarnessDiff, validateDiffOp, loadInterviewerPrompt (5 slugs), loadScript (5 slugs).</done>
</task>

<task id="2" type="auto">
<files>packages/core/tests/onboarding/fixtures/content-creator-happy.json,packages/core/tests/onboarding/fixtures/solo-builder-budget-exceeded.json,packages/core/tests/onboarding/fixtures/generic-abort.json,packages/core/tests/onboarding/fixtures/malformed-response.json</files>
<action>
Criar 4 fixtures de conversa. Cada fixture tem formato:

```json
{
  "description": "...",
  "archetype": "...",
  "budget": { ... },
  "turns": [
    {
      "userInput": "start" | "texto do usuario",
      "modelResponse": "texto bruto que o modelo retornaria (incluindo bloco ```json```)",
      "tokens": { "input": N, "output": N }
    }
  ]
}
```

O teste (task 3) consome `turns[]` na ordem e dispara eventos de stream sinteticos correspondentes.

**Fixture 1: `packages/core/tests/onboarding/fixtures/content-creator-happy.json`**

Conteudo EXATO (JSON valido — note que o campo `modelResponse` contem backticks triplos como string literal):

```json
{
  "description": "Happy path: asking -> asking -> done com diff valido em USER.md",
  "archetype": "content-creator",
  "turns": [
    {
      "userInput": "__START__",
      "modelResponse": "```json\n{ \"status\": \"asking\", \"nextQuestion\": \"Como quer ser chamado?\" }\n```",
      "tokens": { "input": 1200, "output": 80 }
    },
    {
      "userInput": "Me chamo Ana",
      "modelResponse": "```json\n{ \"status\": \"asking\", \"nextQuestion\": \"Qual o nome da sua marca ou perfil?\" }\n```",
      "tokens": { "input": 1300, "output": 90 }
    },
    {
      "userInput": "Marca Acme",
      "modelResponse": "```json\n{ \"status\": \"done\", \"summary\": \"Registrei nome Ana e marca Acme.\", \"harnessDiff\": { \"summary\": \"nome + marca\", \"diffs\": [ { \"file\": \"USER.md\", \"ops\": [ { \"op\": \"set_placeholder\", \"key\": \"userName\", \"value\": \"Ana\" }, { \"op\": \"set_placeholder\", \"key\": \"company\", \"value\": \"Acme\" } ] } ] } }\n```",
      "tokens": { "input": 1400, "output": 300 }
    }
  ]
}
```

**Fixture 2: `packages/core/tests/onboarding/fixtures/solo-builder-budget-exceeded.json`**

```json
{
  "description": "Entrevistador mantem asking alem do maxTurns=2 -> cutoff automatico vira aborted",
  "archetype": "solo-builder",
  "budget": { "maxTurns": 2 },
  "turns": [
    {
      "userInput": "__START__",
      "modelResponse": "```json\n{ \"status\": \"asking\", \"nextQuestion\": \"Nome?\" }\n```",
      "tokens": { "input": 500, "output": 30 }
    },
    {
      "userInput": "Bob",
      "modelResponse": "```json\n{ \"status\": \"asking\", \"nextQuestion\": \"Empresa?\" }\n```",
      "tokens": { "input": 600, "output": 30 }
    },
    {
      "userInput": "nunca deve chegar aqui",
      "modelResponse": "NEVER",
      "tokens": { "input": 0, "output": 0 }
    }
  ]
}
```

**Fixture 3: `packages/core/tests/onboarding/fixtures/generic-abort.json`**

```json
{
  "description": "Entrevistador emite status=aborted no primeiro turno (ex: usuario disse quit)",
  "archetype": "generic",
  "turns": [
    {
      "userInput": "__START__",
      "modelResponse": "```json\n{ \"status\": \"aborted\", \"reason\": \"Usuario pediu para sair antes de iniciar.\" }\n```",
      "tokens": { "input": 400, "output": 40 }
    }
  ]
}
```

**Fixture 4: `packages/core/tests/onboarding/fixtures/malformed-response.json`**

```json
{
  "description": "Modelo retorna texto sem bloco JSON -> deve jogar InterviewResponseParseError",
  "archetype": "generic",
  "turns": [
    {
      "userInput": "__START__",
      "modelResponse": "desculpe, esqueci o formato. vamos la: qual seu nome?",
      "tokens": { "input": 300, "output": 20 }
    }
  ]
}
```

Crie os 4 arquivos exatamente como acima (JSON valido). Valide com:

```bash
for f in content-creator-happy solo-builder-budget-exceeded generic-abort malformed-response; do
  node -e "JSON.parse(require('fs').readFileSync('packages/core/tests/onboarding/fixtures/$f.json','utf8'))" || exit 1
done
echo OK
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && for f in content-creator-happy solo-builder-budget-exceeded generic-abort malformed-response; do test -f "packages/core/tests/onboarding/fixtures/$f.json" && node -e "JSON.parse(require('fs').readFileSync('packages/core/tests/onboarding/fixtures/$f.json','utf8'))" || exit 1; done && echo OK</automated></verify>
<done>4 fixtures JSON criadas, todas parseaveis, cobrindo happy path, budget exceeded, abort e malformed response.</done>
</task>

<task id="3" type="auto">
<files>packages/core/tests/onboarding/interviewer.test.ts</files>
<action>
Criar o teste de integracao da classe `Interviewer` usando mock de `ClaudeRunner` alimentado pelas fixtures.

**Estrategia do mock:**

`vi.mock('../../src/claude-runner', ...)` substitui a classe `ClaudeRunner` globalmente. A implementacao mock expoe um hook `__setNextResponse(text, usage)` que o teste chama antes de cada `start()`/`answer()`. Quando a Interviewer instancia um novo ClaudeRunner e chama `.run(...)`, o mock retorna um AsyncGenerator que emite: um `{type: 'text', data: {text}}` + um `{type: 'done', data: {usage}}`.

Criar `packages/core/tests/onboarding/interviewer.test.ts` com conteudo EXATO:

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { mkdtempSync, writeFileSync, readFileSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';

// --------- Mock do ClaudeRunner ---------
// A instancia real e substituida por uma fake controlada via setNextResponse().
// Cada chamada a `run()` consome a proxima resposta da fila e emite eventos sinteticos.

interface MockResponse {
  text: string;
  usage: {
    input_tokens: number;
    output_tokens: number;
    cache_creation_input_tokens?: number;
    cache_read_input_tokens?: number;
  };
}

const mockResponseQueue: MockResponse[] = [];
let mockCallCount = 0;

function __setNextResponse(r: MockResponse): void {
  mockResponseQueue.push(r);
}
function __resetMock(): void {
  mockResponseQueue.length = 0;
  mockCallCount = 0;
}
function __callCount(): number {
  return mockCallCount;
}

vi.mock('../../src/claude-runner', () => {
  class ClaudeRunner {
    private aborted = false;
    async *run(_prompt: string, _opts: unknown): AsyncGenerator<unknown, void, unknown> {
      mockCallCount += 1;
      const next = mockResponseQueue.shift();
      if (!next) {
        throw new Error('[mock ClaudeRunner] No queued response — test misconfigured');
      }
      if (this.aborted) return;
      yield { type: 'text', data: { text: next.text } };
      yield { type: 'done', data: { usage: next.usage } };
    }
    abort(): void { this.aborted = true; }
    get isRunning(): boolean { return false; }
  }
  return { ClaudeRunner };
});

// IMPORTA APOS vi.mock pra pegar a versao mockada
import { Interviewer } from '../../src/onboarding/interviewer';
import { applyDiff } from '../../src/onboarding/merger';
import { InterviewResponseParseError } from '../../src/onboarding/types';

// --------- Fixtures ---------

interface Fixture {
  description: string;
  archetype: 'solo-builder' | 'content-creator' | 'agency-freela' | 'ecom-manager' | 'generic';
  budget?: { maxTurns?: number; maxInputTokens?: number; maxOutputTokens?: number; timeoutMs?: number };
  turns: Array<{
    userInput: string;
    modelResponse: string;
    tokens: { input: number; output: number };
  }>;
}

const FIXTURES_DIR = join(__dirname, 'fixtures');
function loadFixture(name: string): Fixture {
  return JSON.parse(readFileSync(join(FIXTURES_DIR, `${name}.json`), 'utf-8')) as Fixture;
}

// --------- Helpers de setup do harnessDir temporario ---------

function seedHarness(dir: string): void {
  writeFileSync(
    join(dir, 'USER.md'),
    '# USER.md\n\n- nome: {{userName}}\n- empresa: {{company}}\n- cargo: {{role}}\n- dir: {{workingDir}}\n',
  );
  writeFileSync(join(dir, 'SOUL.md'), '# SOUL\nbase\n');
  writeFileSync(join(dir, 'AGENTS.md'), '# Agents\n');
  writeFileSync(join(dir, 'TOOLS.md'), '# Tools\n');
  writeFileSync(join(dir, 'MEMORY.md'), '# Memory\n');
  writeFileSync(join(dir, 'STYLE.md'), '# Style\n');
  writeFileSync(join(dir, 'HEARTBEAT.md'), '# Heartbeat\n');
}

// --------- Testes ---------

describe('Interviewer integration', () => {
  let tmp: string;

  beforeEach(() => {
    __resetMock();
    tmp = mkdtempSync(join(tmpdir(), 'fc-itv-'));
    seedHarness(tmp);
  });

  it('happy path: asking -> asking -> done and diff applies', async () => {
    const fix = loadFixture('content-creator-happy');
    for (const t of fix.turns) {
      __setNextResponse({
        text: t.modelResponse,
        usage: { input_tokens: t.tokens.input, output_tokens: t.tokens.output },
      });
    }

    const itv = new Interviewer({
      archetype: fix.archetype,
      harnessDir: tmp,
    });

    const r1 = await itv.start();
    expect(r1.status).toBe('asking');
    if (r1.status !== 'asking') throw new Error();
    expect(r1.nextQuestion).toContain('chamado');

    const r2 = await itv.answer('Me chamo Ana');
    expect(r2.status).toBe('asking');

    const r3 = await itv.answer('Marca Acme');
    expect(r3.status).toBe('done');
    if (r3.status !== 'done') throw new Error();

    // Aplicar o diff final em disco
    const mergeResult = applyDiff(tmp, r3.harnessDiff);
    expect(mergeResult.ok).toBe(true);
    expect(mergeResult.appliedFiles).toContain('USER.md');

    const userMd = readFileSync(join(tmp, 'USER.md'), 'utf-8');
    expect(userMd).toContain('nome: Ana');
    expect(userMd).toContain('empresa: Acme');

    const state = itv.getState();
    expect(state.status).toBe('done');
    expect(state.turns.length).toBeGreaterThanOrEqual(4); // 3 interviewer + 2 user (excluindo kickoff)
    expect(state.finalDiff).not.toBeNull();
  });

  it('budget exceeded: maxTurns=2 triggers aborted status before 3rd turn', async () => {
    const fix = loadFixture('solo-builder-budget-exceeded');
    // Somente as 2 primeiras respostas serao consumidas — a 3a NAO deve ser chamada.
    for (const t of fix.turns.slice(0, 2)) {
      __setNextResponse({
        text: t.modelResponse,
        usage: { input_tokens: t.tokens.input, output_tokens: t.tokens.output },
      });
    }

    const itv = new Interviewer({
      archetype: fix.archetype,
      harnessDir: tmp,
      budget: fix.budget,
    });

    const r1 = await itv.start();
    expect(r1.status).toBe('asking');

    const r2 = await itv.answer('Bob');
    // Dependendo de como o budget contar (turno increment antes da chamada),
    // esse 2o turno ja pode ser aborted. Aceite ambos: 'asking' ou 'aborted'.
    expect(['asking', 'aborted']).toContain(r2.status);

    if (r2.status === 'asking') {
      // Proxima tentativa DEVE cair em aborted (max_turns=2 ja atingido apos o 2o incremento)
      const r3 = await itv.answer('nunca chega');
      expect(r3.status).toBe('aborted');
      if (r3.status === 'aborted') {
        expect(r3.reason).toMatch(/budget|max_turns/i);
      }
    }

    const state = itv.getState();
    expect(state.status).toBe('aborted');
    expect(state.budget.withinLimits).toBe(false);
    expect(state.budget.cutoffReason).toBe('max_turns');
  });

  it('model-emitted aborted propagates to state without crash', async () => {
    const fix = loadFixture('generic-abort');
    for (const t of fix.turns) {
      __setNextResponse({
        text: t.modelResponse,
        usage: { input_tokens: t.tokens.input, output_tokens: t.tokens.output },
      });
    }

    const itv = new Interviewer({ archetype: fix.archetype, harnessDir: tmp });
    const r = await itv.start();
    expect(r.status).toBe('aborted');
    if (r.status !== 'aborted') throw new Error();
    expect(r.reason).toMatch(/sair/i);
    expect(itv.getState().status).toBe('aborted');
  });

  it('malformed response throws InterviewResponseParseError', async () => {
    const fix = loadFixture('malformed-response');
    __setNextResponse({
      text: fix.turns[0].modelResponse,
      usage: { input_tokens: fix.turns[0].tokens.input, output_tokens: fix.turns[0].tokens.output },
    });

    const itv = new Interviewer({ archetype: fix.archetype, harnessDir: tmp });
    await expect(itv.start()).rejects.toThrow(InterviewResponseParseError);
    expect(itv.getState().status).toBe('error');
  });

  it('abort() during asking cancels interview', async () => {
    __setNextResponse({
      text: '```json\n{ "status": "asking", "nextQuestion": "q?" }\n```',
      usage: { input_tokens: 100, output_tokens: 30 },
    });

    const itv = new Interviewer({ archetype: 'generic', harnessDir: tmp });
    const r1 = await itv.start();
    expect(r1.status).toBe('asking');

    itv.abort('user-initiated');
    expect(itv.getState().status).toBe('aborted');
    expect(itv.getState().errorMessage).toContain('user-initiated');
  });

  it('getState returns immutable snapshots (different references)', async () => {
    __setNextResponse({
      text: '```json\n{ "status": "asking", "nextQuestion": "q?" }\n```',
      usage: { input_tokens: 100, output_tokens: 30 },
    });
    const itv = new Interviewer({ archetype: 'generic', harnessDir: tmp });
    await itv.start();
    const s1 = itv.getState();
    const s2 = itv.getState();
    expect(s1).not.toBe(s2); // different reference
    expect(s1.turns).not.toBe(s2.turns); // deep copied
  });

  afterEach(() => {
    try { rmSync(tmp, { recursive: true, force: true }); } catch { /* ignore */ }
  });
});

// Importacao de afterEach no escopo do modulo
import { afterEach } from 'vitest';
```

**Nota:** Se o teste de `budget exceeded` falhar pela semantica exata de quando o budget incrementa (antes ou depois do turn), relaxe a expectativa usando `expect(['asking','aborted']).toContain(...)` (ja feito no codigo) e deixe o teste registrar qualquer um dos dois, desde que a interview acabe em `aborted` no final.
</action>
<verify><automated>cd /home/projects/ForgeClaw && test -f packages/core/tests/onboarding/interviewer.test.ts && cd packages/core && (bun x vitest run tests/onboarding/interviewer.test.ts --reporter=basic 2>&1 || npx vitest run tests/onboarding/interviewer.test.ts --reporter=basic 2>&1) | tee /tmp/vitest-26-04-i.log | tail -40 && ! grep -iE "^\s*(failed|failures:)\s+[1-9]" /tmp/vitest-26-04-i.log</automated></verify>
<done>interviewer.test.ts roda verde. Happy path aplica diff em USER.md (substitui placeholders). Budget cutoff vira aborted. Modelo aborted propaga. Malformed response joga InterviewResponseParseError. abort() externo funciona. getState() retorna snapshots imutaveis.</done>
</task>

<task id="4" type="auto">
<files>packages/core/tests/onboarding/prompts.test.ts,packages/core/tests/onboarding/interviewer.test.ts,packages/core/tests/onboarding/budget.test.ts,packages/core/tests/onboarding/merger.test.ts</files>
<action>
Smoke test final: rodar TODA a suite de testes do onboarding e garantir saida verde.

Execute:

```bash
cd /home/projects/ForgeClaw/packages/core && (bun x vitest run tests/onboarding/ --reporter=default 2>&1 || npx vitest run tests/onboarding/ --reporter=default 2>&1) | tee /tmp/vitest-26-04-all.log
```

Interpretar:
- Se `Tests  N passed` sem `failed`, done.
- Se qualquer teste falhar, NAO adicionar `skip`/`todo`. Diagnostique:
  - Erro de `Cannot find module 'vitest'`: a devDep ja deveria estar no package core (ver `packages/core/tests/event-bus.test.ts`). Se nao estiver: `cd /home/projects/ForgeClaw && bun add -D -F @forgeclaw/core vitest`.
  - Erro de types: rodar typecheck filtrado em `onboarding/` e corrigir.
  - Erro de mock: checar que `vi.mock` esta no topo ANTES do `import { Interviewer }`.

Meta: zero falhas.
</action>
<verify><automated>cd /home/projects/ForgeClaw/packages/core && (bun x vitest run tests/onboarding/ --reporter=basic 2>&1 || npx vitest run tests/onboarding/ --reporter=basic 2>&1) | tee /tmp/vitest-26-04-all.log | tail -30 && grep -E "(Tests|Test Files)\s+\d+\s+passed" /tmp/vitest-26-04-all.log && ! grep -iE "^\s*(failed|failures:)\s+[1-9]" /tmp/vitest-26-04-all.log</automated></verify>
<done>vitest run packages/core/tests/onboarding/ passa verde. Todos os testes (prompts, budget, merger, interviewer integration) passam sem failures.</done>
</task>

<task id="5" type="auto">
<files>packages/core/src/onboarding/interviewer.ts,packages/core/src/onboarding/merger.ts,packages/core/src/onboarding/budget.ts,packages/core/src/onboarding/prompts.ts,packages/core/src/onboarding/types.ts</files>
<action>
Typecheck final do package core apos toda a fase:

```bash
cd /home/projects/ForgeClaw && bun tsc --noEmit -p packages/core/tsconfig.json 2>&1 | tee /tmp/tc-final.log | tail -50
```

Filtre apenas erros nos arquivos do onboarding:

```bash
grep -E "onboarding/" /tmp/tc-final.log | grep -E "error TS" | tee /tmp/tc-final-onb.log
```

Se `/tmp/tc-final-onb.log` estiver VAZIO: done.
Se nao, corrigir ANTES de marcar done. Erros em arquivos FORA do onboarding sao pre-existentes e nao bloqueiam.
</action>
<verify><automated>cd /home/projects/ForgeClaw && bun tsc --noEmit -p packages/core/tsconfig.json 2>&1 | tee /tmp/tc-final.log > /dev/null; ONB_ERR=$(grep -E "onboarding/" /tmp/tc-final.log | grep -cE "error TS" || true); test "$ONB_ERR" = "0"</automated></verify>
<done>Typecheck do core nao reporta nenhum erro em arquivos dentro de packages/core/src/onboarding/ ou packages/core/tests/onboarding/.</done>
</task>

## Criterios de Sucesso

- [ ] `packages/core/tests/onboarding/prompts.test.ts` cobre extractJsonBlock (4+ casos), validateInterviewResponse (6+ casos), validateHarnessDiff (3+ casos), validateDiffOp (todas as 4 ops + invalidos), loadInterviewerPrompt/loadScript (5 slugs)
- [ ] `packages/core/tests/onboarding/budget.test.ts` cobre cutoff por turnos, input tokens, output tokens, snapshot imutavel
- [ ] `packages/core/tests/onboarding/merger.test.ts` cobre 4 ops (append, replace, replace_section, set_placeholder) + applyDiff/previewDiff com rollback de arquivo
- [ ] `packages/core/tests/onboarding/interviewer.test.ts` roda Interviewer end-to-end com mock do ClaudeRunner usando 4 fixtures
- [ ] Fixture `content-creator-happy.json` prova: conversa em 3 turnos, diff final aplica em USER.md de verdade
- [ ] Fixture `solo-builder-budget-exceeded.json` prova: maxTurns=2 vira `aborted` com cutoffReason `max_turns`
- [ ] Fixture `generic-abort.json` prova: modelo emite aborted, estado propaga
- [ ] Fixture `malformed-response.json` prova: sem JSON no output -> InterviewResponseParseError
- [ ] `vitest run packages/core/tests/onboarding/` roda verde (0 failures)
- [ ] `bun tsc --noEmit -p packages/core/tsconfig.json` sem erros em onboarding/
