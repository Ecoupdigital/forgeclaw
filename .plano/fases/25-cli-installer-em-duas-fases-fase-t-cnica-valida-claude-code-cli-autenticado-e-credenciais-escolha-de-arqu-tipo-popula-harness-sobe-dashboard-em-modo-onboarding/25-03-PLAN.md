---
phase: 25-cli-installer-em-duas-fases
plan: 25-03
type: feature
autonomous: true
wave: 1
depends_on: ["25-01"]
requirements: [CLI-01, CLI-04]
files_modified:
  - packages/cli/src/commands/install/phase-c-handoff.ts
  - packages/cli/src/commands/install/dashboard-handoff.ts
  - packages/cli/src/utils/open-url.ts
  - packages/cli/tests/install/open-url.test.ts
must_haves:
  truths:
    - "runPhaseC spawna o dashboard via systemctl/launchctl quando serviceInstalled=true, OU via bun run --cwd <dashboard> start --onboarding quando servico ausente"
    - "dashboardIsUp() faz polling em http://localhost:4040/api/health com backoff exponencial ate 15s; falha nao quebra o install (apenas warning)"
    - "openUrl(url) abre o browser via xdg-open (Linux) / open (macOS) / start (Windows), falha silenciosa"
    - "Flag --no-handoff pula o spawn e o open do browser, so imprime URL e token"
    - "Onboarding URL e http://localhost:4040/onboarding?token=<dashboardToken> (dashboard valida via middleware — fase 27)"
    - "state file e limpo (clearState) SO apos handoff completo (spawn + open tentados)"
  artifacts:
    - path: "packages/cli/src/utils/open-url.ts"
      provides: "openUrl(url) portavel (linux/macos/windows) com silenciamento de falhas"
    - path: "packages/cli/src/commands/install/dashboard-handoff.ts"
      provides: "spawnDashboardIfNeeded, waitForDashboardUp, buildOnboardingUrl"
    - path: "packages/cli/src/commands/install/phase-c-handoff.ts (editado)"
      provides: "Fase C real com spawn + health-check + open browser + outro final"
  key_links:
    - from: "phase-c-handoff.ts"
      to: "dashboard-handoff.ts"
      via: "import { spawnDashboardIfNeeded, waitForDashboardUp, buildOnboardingUrl } from './dashboard-handoff'"
    - from: "phase-c-handoff.ts"
      to: "open-url.ts"
      via: "import { openUrl } from '../../utils/open-url'"
    - from: "dashboard-handoff.ts (waitForDashboardUp)"
      to: "dashboard /api/health"
      via: "fetch('http://localhost:4040/api/health', { signal: AbortSignal.timeout(2000) }) com retries"
---

# Fase 25 Plano 03: Handoff pro Dashboard — Spawn + Health-check + Open Browser

**Objetivo:** Implementar a Fase C (handoff pro dashboard) que o 25-01 deixou como stub. Sobe o dashboard (via servico, se instalado; via `bun run start` em background, caso contrario), espera ficar UP (polling em `/api/health`), abre o browser em `http://localhost:4040/onboarding?token=<token>`, imprime outro final. Flag `--no-handoff` desliga o spawn+open mantendo apenas o outro.

## Contexto

@packages/cli/src/commands/install/phase-c-handoff.ts (criado em 25-01 como stub) — recebe (ctx, phaseA, phaseB), imprime outro, chama clearState. Esta fase substitui o corpo.
@packages/cli/src/utils/service.ts — `isServiceRunning`, `startService` existentes. setupService ja instala `forgeclaw-dashboard` systemd unit em Linux. Podemos reutilizar `startService` quando `phaseB.serviceInstalled` for true.
@packages/dashboard/package.json — scripts: `dev` (porta 4040), `build`, `start` (porta 4040). Sem endpoint `/api/health` ainda — Fase 27 cria. Nesta fase, polling em `/api/health` pode falhar silenciosamente; usamos fallback pra `/` (qualquer resposta HTTP < 500 = up).
@packages/dashboard/src/app/ — app directory Next.js. Rotas existentes: api, config, crons, harness, login, memory, page.tsx. Nao tem `onboarding/` ainda (Fase 27). Por enquanto, abrir `http://localhost:4040/onboarding` resulta em 404 — isso e OK, usuario ve a mensagem de setup em progresso da Fase 27 futura.
@packages/core/src/config.ts — `dashboardToken` e `timezone` ja fazem parte do schema. `token` CLI command tambem existe e mostra o token.

## Research

Nivel 1 — verificacao rapida dos comandos de open por plataforma:
- Linux: `xdg-open <url>` (gnome/kde standard), exit 0 normalmente mesmo se nada abre
- macOS: `open <url>`
- Windows: `start "" <url>` (precisa de `shell:true`, nao vamos rodar windows no alpha, mas ficam preparados)

Fallback universal se tudo falhar: imprimir a URL e seguir.

## Tarefas

<task id="1" type="auto">
<files>packages/cli/src/utils/open-url.ts</files>
<action>
Criar util portavel de open URL. Sem dep externa (evitar o pacote `open` do npm — ForgeClaw nao quer deps extras).

Criar `packages/cli/src/utils/open-url.ts` com conteudo EXATO:

```typescript
import { platform } from 'node:os';

export interface OpenUrlResult {
  ok: boolean;
  command?: string;
  reason?: string;
}

/**
 * Abre `url` no browser default. Retorna ok:false silenciosamente se o spawn
 * falhar. NAO e responsabilidade desta funcao validar a URL.
 */
export async function openUrl(url: string, timeoutMs = 5000): Promise<OpenUrlResult> {
  const p = platform();
  let cmd: string[];
  if (p === 'linux') cmd = ['xdg-open', url];
  else if (p === 'darwin') cmd = ['open', url];
  else if (p === 'win32') cmd = ['cmd', '/c', 'start', '""', url];
  else return { ok: false, reason: `Unsupported platform: ${p}` };

  try {
    const proc = Bun.spawn(cmd, {
      stdout: 'ignore',
      stderr: 'pipe',
      stdin: 'ignore',
    });
    const timer = new Promise<'timeout'>((resolve) =>
      setTimeout(() => resolve('timeout'), timeoutMs)
    );
    const done = proc.exited.then(() => 'exit' as const);
    const race = await Promise.race([timer, done]);
    if (race === 'timeout') {
      // xdg-open/open as vezes retorna apos o browser abrir — consideramos ok
      return { ok: true, command: cmd[0] };
    }
    if (proc.exitCode === 0) return { ok: true, command: cmd[0] };
    const stderr = await new Response(proc.stderr).text();
    return {
      ok: false,
      command: cmd[0],
      reason: `${cmd[0]} exited ${proc.exitCode}: ${stderr.slice(0, 200)}`,
    };
  } catch (err) {
    return { ok: false, reason: (err as Error).message };
  }
}
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && test -f packages/cli/src/utils/open-url.ts && grep -q 'export async function openUrl' packages/cli/src/utils/open-url.ts && grep -q "xdg-open" packages/cli/src/utils/open-url.ts && grep -q "'darwin'" packages/cli/src/utils/open-url.ts && grep -q "'win32'" packages/cli/src/utils/open-url.ts && grep -q 'AbortSignal\|Promise.race' packages/cli/src/utils/open-url.ts</automated></verify>
<done>open-url.ts exporta openUrl com branch por platform (linux/darwin/win32), timeout via Promise.race, stderr capturado em falha, retorna OpenUrlResult sem jogar.</done>
</task>

<task id="2" type="auto">
<files>packages/cli/src/commands/install/dashboard-handoff.ts</files>
<action>
Criar modulo que encapsula: (a) spawn do dashboard em background quando nao ha servico, (b) polling ate ficar up, (c) builder de URL de onboarding.

Criar `packages/cli/src/commands/install/dashboard-handoff.ts` com conteudo EXATO:

```typescript
import { resolve } from 'node:path';
import { startService, isServiceRunning } from '../../utils/service';

export const DASHBOARD_HOST = 'localhost';
export const DASHBOARD_PORT = 4040;

export interface SpawnResult {
  strategy: 'service-started' | 'service-already-up' | 'background-spawn' | 'skipped';
  pid?: number;
  message: string;
}

/**
 * Em cenario ideal o servico systemd/launchd ja foi instalado em Fase B,
 * entao apenas startamos (ou consultamos se ja sobe). Fallback: spawn em background
 * rodando `bun run start` no diretorio do dashboard.
 */
export async function spawnDashboardIfNeeded(
  monorepoRoot: string,
  serviceInstalled: boolean
): Promise<SpawnResult> {
  if (serviceInstalled) {
    const running = await isServiceRunning();
    if (running) {
      return { strategy: 'service-already-up', message: 'forgeclaw service already running.' };
    }
    const r = await startService();
    if (r.success) {
      return { strategy: 'service-started', message: r.message };
    }
    // Falhou a subir servico — nao fatal, tenta fallback
    // eslint-disable-next-line no-console
    console.warn(`[handoff] startService failed: ${r.message} — falling back to background spawn.`);
  }

  // Fallback: background spawn do dashboard em modo producao (se build existe) ou dev
  const dashboardDir = resolve(monorepoRoot, 'packages', 'dashboard');
  try {
    const proc = Bun.spawn(['bun', 'run', 'start'], {
      cwd: dashboardDir,
      stdout: 'ignore',
      stderr: 'ignore',
      stdin: 'ignore',
      // Desacoplar do processo pai pra sobreviver apos `forgeclaw install` terminar
      env: { ...process.env, NODE_ENV: 'production' },
    });
    // Pequena espera pra processo falhar imediatamente se start nao existir
    await Promise.race([
      new Promise((r) => setTimeout(r, 800)),
      proc.exited,
    ]);
    if (proc.exitCode !== null && proc.exitCode !== 0) {
      return {
        strategy: 'skipped',
        message: `Dashboard spawn exited ${proc.exitCode}. Start it manually: cd packages/dashboard && bun run start`,
      };
    }
    return {
      strategy: 'background-spawn',
      pid: proc.pid,
      message: `Dashboard spawned in background (pid ${proc.pid}).`,
    };
  } catch (err) {
    return {
      strategy: 'skipped',
      message: `Failed to spawn dashboard: ${(err as Error).message}`,
    };
  }
}

/**
 * Polla o dashboard com backoff exponencial ate ficar up ou timeout.
 * Primeiro tenta /api/health (Fase 27). Se 404, aceita qualquer resposta < 500 em /.
 */
export async function waitForDashboardUp(maxMs = 15000): Promise<boolean> {
  const start = Date.now();
  let delay = 400;
  const probes = [
    `http://${DASHBOARD_HOST}:${DASHBOARD_PORT}/api/health`,
    `http://${DASHBOARD_HOST}:${DASHBOARD_PORT}/`,
  ];
  while (Date.now() - start < maxMs) {
    for (const url of probes) {
      try {
        const res = await fetch(url, { signal: AbortSignal.timeout(1500) });
        if (res.status < 500) return true;
      } catch {
        // connection refused / timeout — continua polling
      }
    }
    await new Promise((r) => setTimeout(r, delay));
    delay = Math.min(delay * 1.5, 2500);
  }
  return false;
}

/**
 * Constroi a URL de onboarding para o browser.
 */
export function buildOnboardingUrl(token: string): string {
  const safe = encodeURIComponent(token);
  return `http://${DASHBOARD_HOST}:${DASHBOARD_PORT}/onboarding?token=${safe}`;
}
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && test -f packages/cli/src/commands/install/dashboard-handoff.ts && grep -q 'export async function spawnDashboardIfNeeded' packages/cli/src/commands/install/dashboard-handoff.ts && grep -q 'export async function waitForDashboardUp' packages/cli/src/commands/install/dashboard-handoff.ts && grep -q 'export function buildOnboardingUrl' packages/cli/src/commands/install/dashboard-handoff.ts && grep -q "DASHBOARD_PORT = 4040" packages/cli/src/commands/install/dashboard-handoff.ts && grep -q "/api/health" packages/cli/src/commands/install/dashboard-handoff.ts && grep -q "/onboarding" packages/cli/src/commands/install/dashboard-handoff.ts</automated></verify>
<done>dashboard-handoff.ts exporta spawnDashboardIfNeeded (systemctl start primeiro, fallback para bun run start), waitForDashboardUp (polling backoff exponencial ate 15s, probes /api/health e /), buildOnboardingUrl (encodeURIComponent do token).</done>
</task>

<task id="3" type="auto">
<files>packages/cli/src/commands/install/phase-c-handoff.ts</files>
<action>
Substituir o STUB de `runPhaseC` por implementacao real. A funcao recebe (ctx, phaseA, phaseB), decide se spawna dashboard (baseado em `phaseB.serviceInstalled`), polla ate up, abre browser, imprime outro, limpa state.

Substituir o arquivo INTEIRO pelo conteudo abaixo:

```typescript
import { outro, log, spinner } from '@clack/prompts';
import type { InstallContext, PhaseAResult, PhaseBResult } from './types';
import { clearState } from './state';
import {
  spawnDashboardIfNeeded,
  waitForDashboardUp,
  buildOnboardingUrl,
} from './dashboard-handoff';
import { openUrl } from '../../utils/open-url';

function formatFinalOutro(
  phaseA: PhaseAResult,
  phaseB: PhaseBResult,
  url: string,
  dashboardReady: boolean,
  handoffSkipped: boolean
): string {
  const readyLine = dashboardReady
    ? `Dashboard: ${url}`
    : `Dashboard did not respond yet. Try opening manually: ${url}`;
  const handoffLine = handoffSkipped
    ? 'Handoff skipped (--no-handoff). Open the URL above manually when ready.'
    : 'The dashboard should be opening in your browser.';

  return `ForgeClaw is ready!

  Archetype: ${phaseB.archetype}
  Harness files: ${phaseB.harnessFilesWritten.length}
  ${readyLine}
  Dashboard Token: ${phaseA.dashboardToken}

  Open Telegram and send /start to your bot
  Status: forgeclaw status
  Logs:   forgeclaw logs

  API keys stored in: ~/.forgeclaw/.env
  Config:            ~/.forgeclaw/forgeclaw.config.json

  ${handoffLine}

  IMPORTANT: Save the Dashboard Token above. You'll need it to access the dashboard.`;
}

export async function runPhaseC(
  ctx: InstallContext,
  phaseA: PhaseAResult,
  phaseB: PhaseBResult
): Promise<void> {
  const url = buildOnboardingUrl(phaseA.dashboardToken);

  if (ctx.options.noHandoff) {
    log.info('Skipping dashboard handoff (--no-handoff).');
    outro(formatFinalOutro(phaseA, phaseB, url, false, true));
    clearState(ctx.stateFilePath);
    return;
  }

  // --- 1) Spawn dashboard ---
  const spSpawn = spinner();
  spSpawn.start('Starting dashboard...');
  const spawnResult = await spawnDashboardIfNeeded(
    ctx.monorepoRoot,
    phaseB.serviceInstalled
  );
  spSpawn.stop(`Dashboard: ${spawnResult.strategy} — ${spawnResult.message}`);

  // --- 2) Wait until up ---
  const spWait = spinner();
  spWait.start('Waiting for dashboard to accept connections...');
  const up = await waitForDashboardUp(15000);
  spWait.stop(up ? 'Dashboard is up.' : 'Dashboard did not respond within 15s.');

  // --- 3) Open browser ---
  if (up) {
    const openResult = await openUrl(url);
    if (!openResult.ok) {
      log.warn(`Could not open browser automatically: ${openResult.reason ?? 'unknown'}`);
      log.info(`Open manually: ${url}`);
    } else {
      log.success(`Opened browser via ${openResult.command}.`);
    }
  } else {
    log.warn(`Dashboard not responding. Open manually after it starts: ${url}`);
  }

  // --- 4) Outro final ---
  outro(formatFinalOutro(phaseA, phaseB, url, up, false));

  // --- 5) Limpar state apos handoff ---
  clearState(ctx.stateFilePath);
}
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -q "export async function runPhaseC" packages/cli/src/commands/install/phase-c-handoff.ts && grep -q "spawnDashboardIfNeeded" packages/cli/src/commands/install/phase-c-handoff.ts && grep -q "waitForDashboardUp" packages/cli/src/commands/install/phase-c-handoff.ts && grep -q "buildOnboardingUrl" packages/cli/src/commands/install/phase-c-handoff.ts && grep -q "openUrl" packages/cli/src/commands/install/phase-c-handoff.ts && grep -q "ctx.options.noHandoff" packages/cli/src/commands/install/phase-c-handoff.ts && grep -q "clearState" packages/cli/src/commands/install/phase-c-handoff.ts</automated></verify>
<done>phase-c-handoff.ts implementa fluxo completo: noHandoff short-circuit imprime outro e limpa state; senao spawn -> waitForUp (15s) -> openUrl -> outro -> clearState. Mensagens refletem cada strategy/estado.</done>
</task>

<task id="4" type="auto">
<files>packages/cli/tests/install/open-url.test.ts</files>
<action>
Criar teste leve do open-url. O teste nao abre browser de verdade (seria nao-deterministico no CI). Ele apenas valida o shape do retorno e o tratamento de platform/binary ausente.

Criar `packages/cli/tests/install/open-url.test.ts` com conteudo EXATO:

```typescript
import { describe, test, expect } from 'bun:test';
import { openUrl } from '../../src/utils/open-url';

describe('openUrl', () => {
  test('returns OpenUrlResult shape', async () => {
    // Usa uma URL bogus — nao importa; o teste valida o shape do retorno.
    // Em CI sem DISPLAY, xdg-open costuma sair != 0; em maquina com DISPLAY, sai 0.
    // Ambos sao aceitaveis desde que o retorno seja { ok, command, reason? }.
    const r = await openUrl('http://localhost:65530/__test__', 3000);
    expect(typeof r.ok).toBe('boolean');
    if (r.command) expect(typeof r.command).toBe('string');
    if (!r.ok && r.reason) expect(typeof r.reason).toBe('string');
  }, 10000);

  test('timeout nao quebra a funcao', async () => {
    // Mesmo URL valido, forcamos timeout curto: retorno e ok:true (timeout e tratado como success optimista).
    const r = await openUrl('http://localhost:65530/__test__', 50);
    expect(typeof r.ok).toBe('boolean');
  });
});
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && test -f packages/cli/tests/install/open-url.test.ts && bun test packages/cli/tests/install/open-url.test.ts 2>&1 | tail -10 | tee /tmp/openurl.log && ! grep -iE "\bfail\b[^:]*:\s+[1-9]" /tmp/openurl.log</automated></verify>
<done>open-url.test.ts roda verde. Valida shape do retorno e tolera timeout curto sem explodir.</done>
</task>

<task id="5" type="auto">
<files>packages/cli/src/commands/install/phase-c-handoff.ts,packages/cli/src/commands/install/dashboard-handoff.ts,packages/cli/src/utils/open-url.ts</files>
<action>
Smoke test final do handoff:

1. **Typecheck** — garantir que todos os arquivos novos compilam:
   ```bash
   cd /home/projects/ForgeClaw && bun tsc --noEmit -p packages/cli/tsconfig.json 2>&1 | grep -E "install/(phase-c-handoff|dashboard-handoff)\.ts|utils/open-url\.ts" | grep -v "Found 0 errors"
   ```
   Zero linhas de saida = OK.

2. **Teste integrado (dry-run sem handoff)** — rodar `install --no-handoff` com bot token fake nao e viavel (prompts interativos + writeState real). Em vez disso, valida-se via testes unit de dashboard-handoff:

   Criar um teste inline rapido que valida `buildOnboardingUrl` encode corretamente o token:
   ```bash
   cd /home/projects/ForgeClaw && bun -e "import {buildOnboardingUrl} from './packages/cli/src/commands/install/dashboard-handoff'; const u = buildOnboardingUrl('abc 123+xyz'); if (!u.includes('/onboarding?token=abc%20123%2Bxyz')) { console.error('FAIL:', u); process.exit(1) } console.log('OK:', u);"
   ```

3. **waitForDashboardUp retorna false em porta desconectada**:
   ```bash
   cd /home/projects/ForgeClaw && bun -e "import {waitForDashboardUp} from './packages/cli/src/commands/install/dashboard-handoff'; const r = await waitForDashboardUp(2000); if (r !== false) { console.error('FAIL expected false, got', r); process.exit(1) } console.log('OK false');"
   ```
   (Assumindo que nao ha dashboard rodando na 4040 durante os testes; se houver, o teste retorna true — ainda aceitavel.)

Se algum dos tres checks falhar, corrigir antes de marcar done.
</action>
<verify><automated>cd /home/projects/ForgeClaw && (bun tsc --noEmit -p packages/cli/tsconfig.json 2>&1 | grep -E "install/(phase-c-handoff|dashboard-handoff)\.ts|utils/open-url\.ts" | grep -v "Found 0 errors"); EXIT=$?; if [ $EXIT -eq 1 ]; then echo TYPES_OK; else echo TYPES_FAIL; exit 1; fi; bun -e "import {buildOnboardingUrl} from './packages/cli/src/commands/install/dashboard-handoff'; const u = buildOnboardingUrl('abc 123+xyz'); if (!u.includes('/onboarding?token=abc%20123%2Bxyz')) { console.error('FAIL:', u); process.exit(1) } console.log('URL_OK');" && bun test packages/cli/tests/install/open-url.test.ts 2>&1 | tail -10 | grep -q '0 fail\|pass' && echo ALL_SMOKE_OK</automated></verify>
<done>Typecheck limpo, buildOnboardingUrl codifica especiais corretamente (espaco -> %20, + -> %2B), open-url unit test verde.</done>
</task>

## Criterios de Sucesso

- [ ] `packages/cli/src/utils/open-url.ts` abre URLs em linux/macos/windows sem dep externa, silencia falhas
- [ ] `packages/cli/src/commands/install/dashboard-handoff.ts` exporta spawnDashboardIfNeeded (service start com fallback bun run start), waitForDashboardUp (polling backoff ate 15s), buildOnboardingUrl (encode de token)
- [ ] `runPhaseC` real spawna (ou pula se --no-handoff), espera up, tenta abrir browser, imprime outro completo, limpa state file
- [ ] Quando `phaseB.serviceInstalled=true`, handoff usa `startService`; quando false, usa background spawn em `packages/dashboard`
- [ ] URL de onboarding inclui `?token=<encoded>` pra autenticacao posterior do dashboard (Fase 27)
- [ ] `--no-handoff` pula spawn e open mas imprime URL+token no outro
- [ ] `clearState` e chamado APOS outro final em todos os caminhos (noHandoff e normal)
- [ ] `bun test packages/cli/tests/install/open-url.test.ts` verde
- [ ] `bun tsc --noEmit -p packages/cli/tsconfig.json` sem novos erros nos arquivos
