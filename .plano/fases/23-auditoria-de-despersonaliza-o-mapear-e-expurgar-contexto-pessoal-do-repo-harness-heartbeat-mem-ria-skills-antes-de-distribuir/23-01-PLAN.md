---
phase: 23-auditoria-de-despersonalizacao
plan: 23-01
type: chore
autonomous: true
wave: 0
depends_on: []
requirements: []
files_modified:
  - scripts/audit-personal-context.ts  # NEW — auditor automatizado
  - .plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/AUDIT-REPORT.md  # NEW — relatório gerado
  - .plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/CLEANUP-CHECKLIST.md  # NEW — checklist derivado
  - package.json  # ADD script "audit:personal"
must_haves:
  truths:
    - "Script audit-personal-context.ts roda standalone via bun e varre o repo inteiro"
    - "AUDIT-REPORT.md lista todo arquivo com PII/contexto pessoal, com path absoluto, linha, categoria e trecho"
    - "CLEANUP-CHECKLIST.md enumera ações de remoção/sanitização por arquivo, priorizadas por criticidade"
    - "Zero falso-negativo para as 10 categorias conhecidas (nome, empresa, paths, bot handle, user id, URLs de repo, emails, clientes LFpro/Kovvy/DonVicente, skills privadas, screenshots)"
  artifacts:
    - path: "scripts/audit-personal-context.ts"
      provides: "Scanner de regex multi-categoria com output JSON + markdown"
    - path: ".plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/AUDIT-REPORT.md"
      provides: "Snapshot legível do que vaza e onde"
    - path: ".plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/CLEANUP-CHECKLIST.md"
      provides: "Ações acionáveis para o plano 23-02 executar"
  key_links:
    - from: "scripts/audit-personal-context.ts"
      to: "AUDIT-REPORT.md"
      via: "write do output do scanner ao final da execução"
    - from: "AUDIT-REPORT.md"
      to: "CLEANUP-CHECKLIST.md"
      via: "derivação manual (ou script helper) agrupando findings por ação"
    - from: "package.json"
      to: "scripts/audit-personal-context.ts"
      via: "npm script `audit:personal` → `bun run scripts/audit-personal-context.ts`"
---

# Fase 23 Plano 23-01: Auditoria Automatizada de Contexto Pessoal

**Objetivo:** Construir um scanner determinístico que varre o repo ForgeClaw inteiro em busca de contexto pessoal do Jonathan (nomes próprios, empresas, clientes, paths hardcoded, handles, tokens, URLs privadas) e gera dois artefatos: um **AUDIT-REPORT.md** (inventário cru) e um **CLEANUP-CHECKLIST.md** (ações agrupadas). O output é entrada direta do plano 23-02 (remoções) e do 23-03 (CI guard).

Este plano **não remove nada**. Só mapeia. Produz ground truth auditável.

## Contexto

Fases 1-22 do ForgeClaw rodaram com o repo sob uso pessoal do Jonathan. Vazou contexto em múltiplos lugares:

@/home/projects/ForgeClaw/packages/core/src/memory/prompts/janitor.md — path `/home/vault/05-pessoal/daily-log`, nome "Jonathan", entidades "lfpro/dra nathalia/don vicente"
@/home/projects/ForgeClaw/packages/core/src/memory/prompts/writer.md — idem, exemplos com "gestor-lfpro"
@/home/projects/ForgeClaw/packages/core/src/memory-manager.ts — comentário `// Jonathan is in BRT (UTC-3)`
@/home/projects/ForgeClaw/packages/dashboard/src/lib/mock-data.ts — `vaultPath: "/home/vault"`, mock content em pt-BR referenciando o projeto atual
@/home/projects/ForgeClaw/.continue-aqui.md — bot handle `@ForgeClawUP_bot`, user id `450030767`, token mascarado `8662287719:...`, vault `/home/vault`, URL `github.com/Ecoupdigital/forgeclaw`
@/home/projects/ForgeClaw/.playwright-mcp/ — ~10 yml + logs de sessões reais do dashboard com mensagens tipo "Oi, Jonathan!", "Você é o **Jonathan Renan Outeiro** — fundador da EcoUp Digital, de Novo Hamburgo/RS", paths `/home/projects/gestor-lfpro/`, `/home/projects/bv-otica-crm/`, `/home/projects/inbox-deal-flow/`
@/home/projects/ForgeClaw/ops/forgeclaw.service — `WorkingDirectory=/home/projects/ForgeClaw`, `/root/.bun/bin/bun`
@/home/projects/ForgeClaw/ops/forgeclaw-dashboard.service — mesmo pattern
@/home/projects/ForgeClaw/packages/cli/src/commands/install.ts — defaults `/root/projects` (L178), `/root/obsidian` (L199)
@/home/projects/ForgeClaw/packages/core/src/context-builder.ts — path `{vaultPath}/05-pessoal/daily-log` (estrutura Obsidian específica do Jonathan)
@/home/projects/ForgeClaw/README.md — `git clone https://github.com/Ecoupdigital/forgeclaw.git` (x2), `/home/usuario/obsidian` (placeholder, OK)
@/home/projects/ForgeClaw/.plano/STATE.md — `Repo: github.com/Ecoupdigital/forgeclaw`
@/home/projects/ForgeClaw/.plano/BRIEFING-M.md — já documentou M1 (daily log dir hardcoded pra vault pessoal)
@/home/projects/ForgeClaw/packages/dashboard/src/app/api/skills/route.ts — aponta pra `~/.claude/skills/` (acopla produto a skills privadas do user, não do ForgeClaw)

## Research

Pesquisa Nivel 0 (pulada). Todo trabalho interno: ler arquivos e regex. Biblioteca padrão Bun + Node `node:fs` / `node:path` resolve. Não precisa de WebFetch/Context7.

## Tarefas

<task id="1" type="auto">
<files>scripts/audit-personal-context.ts</files>
<action>
Criar script TypeScript standalone executável via `bun run scripts/audit-personal-context.ts`. Estrutura:

```typescript
#!/usr/bin/env bun
// scripts/audit-personal-context.ts
// Scanner determinístico de contexto pessoal no repo ForgeClaw.
// Uso: bun run scripts/audit-personal-context.ts [--json] [--out=<path>]

import { readdir, readFile, writeFile, stat } from 'node:fs/promises';
import { join, relative, extname } from 'node:path';

const REPO_ROOT = process.cwd(); // espera-se rodar da raiz do repo

// Pastas ignoradas SEMPRE (ruído puro)
const IGNORE_DIRS = new Set([
  'node_modules', '.git', 'dist', '.next', 'coverage',
  '.playwright-mcp', // ESTES são alvos do plano 23-02 (deleção), mas o scanner NÃO deve vasculhar dentro — só registra existência
  '.gitnexus',
]);

// Extensões que valem ler
const TEXT_EXTS = new Set([
  '.ts', '.tsx', '.js', '.jsx', '.md', '.mdx',
  '.json', '.yml', '.yaml', '.service', '.toml',
  '.html', '.css', '.sh', '.env', '.example',
]);

type Category =
  | 'personal_name'
  | 'personal_company'
  | 'personal_client'
  | 'personal_handle'
  | 'personal_userid'
  | 'hardcoded_path'
  | 'private_repo_url'
  | 'private_skill_dep'
  | 'vault_structure'
  | 'playwright_snapshot'
  | 'bot_token_fragment';

interface Finding {
  file: string;           // path relativo ao REPO_ROOT
  line: number;           // 1-based
  category: Category;
  pattern: string;        // regex que deu match
  snippet: string;        // contexto truncado 200 chars
  severity: 'critical' | 'high' | 'medium' | 'low';
}

// Categorias + severidade + regex.
// Patterns conservadores — preferem false-positive a false-negative.
// Observação: alguns patterns aparecem em código de teste/comentário
// legítimos (ex: `/home/usuario/obsidian` é placeholder ok).
// O relator classifica, o humano decide.
const RULES: Array<{ category: Category; severity: Finding['severity']; pattern: RegExp; note?: string }> = [
  // personal_name — case-insensitive
  { category: 'personal_name', severity: 'critical', pattern: /\bJonathan\b/g },
  { category: 'personal_name', severity: 'critical', pattern: /\bJonathan\s+Renan\b/gi },
  { category: 'personal_name', severity: 'high',     pattern: /\bdra\s+nathalia\b/gi },

  // personal_company
  { category: 'personal_company', severity: 'critical', pattern: /\bEcoUp\b/g },
  { category: 'personal_company', severity: 'critical', pattern: /\bEcoupdigital\b/gi },
  { category: 'personal_company', severity: 'high',     pattern: /\becoup\.digital\b/gi },

  // personal_client — nomes de clientes/projetos do Jonathan
  { category: 'personal_client', severity: 'critical', pattern: /\b(lfpro|LFpro|LF\s?Pro|gestor-lfpro)\b/g },
  { category: 'personal_client', severity: 'critical', pattern: /\b(kovvy|Kovvy|clearify|Clearify)\b/g },
  { category: 'personal_client', severity: 'critical', pattern: /\b(don[\s-]?vicente)\b/gi },
  { category: 'personal_client', severity: 'critical', pattern: /\b(bv-otica|bv_otica|BV\s?[OÓ]tica)\b/gi },
  { category: 'personal_client', severity: 'critical', pattern: /\b(foco-real|focoreal)\b/gi },
  { category: 'personal_client', severity: 'critical', pattern: /\b(velhos-parceiros|velhos_parceiros)\b/gi },
  { category: 'personal_client', severity: 'high',     pattern: /\b(passini|mybrows|transplant|enove)\b/gi },

  // personal_handle
  { category: 'personal_handle', severity: 'critical', pattern: /@ForgeClawUP_bot/g },
  { category: 'personal_handle', severity: 'high',     pattern: /@jonathanrenan\.ia/g },

  // personal_userid — id do Jonathan
  { category: 'personal_userid', severity: 'critical', pattern: /\b450030767\b/g },
  { category: 'personal_userid', severity: 'high',     pattern: /\b1087968824\b/g }, // group anonymous bot mencionado em .continue-aqui

  // hardcoded_path — paths que só existem na máquina do Jonathan
  { category: 'hardcoded_path', severity: 'critical', pattern: /\/home\/vault\b/g },
  { category: 'hardcoded_path', severity: 'high',     pattern: /\/home\/projects\/(?!ForgeClaw\b)[A-Za-z0-9_-]+/g }, // /home/projects/X onde X != ForgeClaw
  { category: 'hardcoded_path', severity: 'high',     pattern: /\/home\/projects\/ForgeClaw\b/g }, // presente em systemd — precisa ser variável
  { category: 'hardcoded_path', severity: 'medium',   pattern: /\/root\/projects\b/g },
  { category: 'hardcoded_path', severity: 'medium',   pattern: /\/root\/obsidian\b/g },

  // private_repo_url
  { category: 'private_repo_url', severity: 'critical', pattern: /github\.com\/Ecoupdigital\/forgeclaw/gi },

  // private_skill_dep — acoplamento a skills pessoais
  { category: 'private_skill_dep', severity: 'high', pattern: /\basaas[-_]?(api|mcp)\b/gi },
  { category: 'private_skill_dep', severity: 'high', pattern: /\buazapi[-_]?(mcp)?\b/gi },
  { category: 'private_skill_dep', severity: 'high', pattern: /\bconta[-_]?azul\b/gi },
  { category: 'private_skill_dep', severity: 'medium', pattern: /~\/\.claude\/skills\b/g }, // api/skills/route.ts aponta pra cá — OK mas precisa decisão

  // vault_structure — estrutura específica do vault do Jonathan
  { category: 'vault_structure', severity: 'high', pattern: /05-pessoal\/daily-log/g },
  { category: 'vault_structure', severity: 'medium', pattern: /\b0\d-(inbox|projetos|clientes|skills|empresa|pessoal|conte[uú]do|conhecimentos)\b/gi },

  // bot_token_fragment — prefixos de token exposto
  { category: 'bot_token_fragment', severity: 'critical', pattern: /\b8662287719:/g },
  // pattern genérico de bot token (número:alfanum) — só em .continue-aqui deve bater
  { category: 'bot_token_fragment', severity: 'medium', pattern: /\b\d{9,10}:[A-Za-z0-9_-]{10,}/g },
];

function detectPlaywright(path: string): Finding | null {
  // A pasta .playwright-mcp está no IGNORE, mas queremos registrar
  // que ela existe pra o checklist. Isto é feito no walker, não aqui.
  return null;
}

async function walk(dir: string, out: string[]): Promise<void> {
  const entries = await readdir(dir, { withFileTypes: true });
  for (const e of entries) {
    if (e.name.startsWith('.') && IGNORE_DIRS.has(e.name)) continue;
    if (IGNORE_DIRS.has(e.name)) continue;
    const full = join(dir, e.name);
    if (e.isDirectory()) {
      await walk(full, out);
    } else if (e.isFile()) {
      const ext = extname(e.name).toLowerCase();
      // Arquivos sem extensão (tipo LICENSE, Dockerfile) — inclui
      if (TEXT_EXTS.has(ext) || !ext) {
        const st = await stat(full);
        if (st.size <= 5_000_000) out.push(full);
      }
    }
  }
}

async function scanFile(abs: string): Promise<Finding[]> {
  const rel = relative(REPO_ROOT, abs);
  const content = await readFile(abs, 'utf-8');
  const lines = content.split('\n');
  const findings: Finding[] = [];
  for (const rule of RULES) {
    rule.pattern.lastIndex = 0;
    for (let i = 0; i < lines.length; i++) {
      const line = lines[i];
      rule.pattern.lastIndex = 0;
      let m: RegExpExecArray | null;
      while ((m = rule.pattern.exec(line)) !== null) {
        findings.push({
          file: rel,
          line: i + 1,
          category: rule.category,
          pattern: rule.pattern.source,
          snippet: line.trim().slice(0, 200),
          severity: rule.severity,
        });
        if (!rule.pattern.global) break;
      }
    }
  }
  return findings;
}

async function registerPlaywrightDir(out: Finding[]): Promise<void> {
  const playDir = join(REPO_ROOT, '.playwright-mcp');
  try {
    const st = await stat(playDir);
    if (st.isDirectory()) {
      const entries = await readdir(playDir);
      out.push({
        file: '.playwright-mcp/',
        line: 0,
        category: 'playwright_snapshot',
        pattern: 'directory_presence',
        snippet: `diretório contém ${entries.length} snapshots de sessões do dashboard (dados reais)`,
        severity: 'critical',
      });
    }
  } catch { /* dir não existe, tudo bem */ }
}

async function main(): Promise<void> {
  const args = process.argv.slice(2);
  const wantJson = args.includes('--json');
  const outArg = args.find(a => a.startsWith('--out='))?.split('=')[1];

  const files: string[] = [];
  await walk(REPO_ROOT, files);

  const all: Finding[] = [];
  for (const f of files) {
    try {
      const fs = await scanFile(f);
      all.push(...fs);
    } catch (err) {
      console.error(`[audit] falha lendo ${f}:`, err);
    }
  }
  await registerPlaywrightDir(all);

  // Ordena: severity desc, depois file, depois line
  const sevOrder: Record<Finding['severity'], number> = { critical: 0, high: 1, medium: 2, low: 3 };
  all.sort((a, b) => sevOrder[a.severity] - sevOrder[b.severity] || a.file.localeCompare(b.file) || a.line - b.line);

  if (wantJson) {
    const payload = JSON.stringify(all, null, 2);
    if (outArg) await writeFile(outArg, payload, 'utf-8');
    else console.log(payload);
    return;
  }

  // Markdown output
  const byFile = new Map<string, Finding[]>();
  for (const f of all) {
    const arr = byFile.get(f.file) ?? [];
    arr.push(f);
    byFile.set(f.file, arr);
  }

  const lines: string[] = [];
  lines.push('# Audit Report — Contexto Pessoal no Repo ForgeClaw');
  lines.push('');
  lines.push(`Gerado em: ${new Date().toISOString()}`);
  lines.push(`Arquivos varridos: ${files.length}`);
  lines.push(`Findings totais: ${all.length}`);
  lines.push('');
  const bySev: Record<Finding['severity'], number> = { critical: 0, high: 0, medium: 0, low: 0 };
  for (const f of all) bySev[f.severity]++;
  lines.push('## Sumário por severidade');
  lines.push('');
  lines.push(`- critical: ${bySev.critical}`);
  lines.push(`- high: ${bySev.high}`);
  lines.push(`- medium: ${bySev.medium}`);
  lines.push(`- low: ${bySev.low}`);
  lines.push('');
  const byCat: Record<string, number> = {};
  for (const f of all) byCat[f.category] = (byCat[f.category] ?? 0) + 1;
  lines.push('## Sumário por categoria');
  lines.push('');
  for (const [c, n] of Object.entries(byCat).sort((a, b) => b[1] - a[1])) {
    lines.push(`- ${c}: ${n}`);
  }
  lines.push('');
  lines.push('## Findings por arquivo');
  lines.push('');
  const sortedFiles = [...byFile.keys()].sort();
  for (const file of sortedFiles) {
    const fs = byFile.get(file)!;
    const maxSev = fs.reduce<Finding['severity']>((acc, x) =>
      sevOrder[x.severity] < sevOrder[acc] ? x.severity : acc, 'low');
    lines.push(`### \`${file}\` (${fs.length} findings, pior: ${maxSev})`);
    lines.push('');
    lines.push('| Linha | Sev | Categoria | Pattern | Snippet |');
    lines.push('|-------|-----|-----------|---------|---------|');
    for (const f of fs) {
      const snip = f.snippet.replace(/\|/g, '\\|');
      lines.push(`| ${f.line} | ${f.severity} | ${f.category} | \`${f.pattern}\` | \`${snip}\` |`);
    }
    lines.push('');
  }

  const md = lines.join('\n');
  if (outArg) await writeFile(outArg, md, 'utf-8');
  else console.log(md);
}

main().catch((err) => {
  console.error('[audit] fatal:', err);
  process.exit(1);
});
```

Regras de engenharia:
- Usa **somente** APIs `node:fs/promises` e `node:path`. Nada de dependências novas.
- Ignora `node_modules`, `.git`, `dist`, `.next`, `coverage`, `.gitnexus`. Para `.playwright-mcp` **não lê o conteúdo** (evita poluir findings com KB de HTML), só registra a existência como 1 finding critical.
- Limita tamanho de arquivo a 5 MB (evita `bun.lock`).
- Patterns globais `/...g` com reset explícito de `lastIndex` a cada linha (proteção contra bug clássico do regex stateful).
- Cada rule deve ter `severity`; se houver dúvida, marca `high` (conservador).
- Output markdown é auto-suficiente (sumário, por categoria, por arquivo).
- `--json` troca pra JSON; `--out=X` escreve no path X em vez de stdout.
- Exit code 0 mesmo com findings (é só inventário). Exit 1 só em erro fatal.
</action>
<verify>
  <automated>cd /home/projects/ForgeClaw && bun run scripts/audit-personal-context.ts --out=/tmp/audit-dry.md && test -s /tmp/audit-dry.md && grep -q 'Audit Report' /tmp/audit-dry.md && grep -q 'personal_name' /tmp/audit-dry.md && grep -q 'hardcoded_path' /tmp/audit-dry.md</automated>
</verify>
<done>Script roda sem erro, gera markdown válido com >=1 finding nas categorias personal_name e hardcoded_path (sabemos que existem em janitor.md, writer.md, memory-manager.ts, mock-data.ts, .continue-aqui.md).</done>
</task>

<task id="2" type="auto">
<files>package.json</files>
<action>
Adicionar script `audit:personal` ao bloco `scripts` do `package.json` da raiz do monorepo. O script chama `bun run scripts/audit-personal-context.ts`. Preservar todos os scripts existentes. Não tocar em `name`, `version`, `workspaces`, `dependencies`, `devDependencies`, `packageManager`.

Exemplo de diff conceitual (aplicar via Edit tool, não substituir o arquivo inteiro):

```json
{
  "scripts": {
    "...scripts existentes...": "...",
    "audit:personal": "bun run scripts/audit-personal-context.ts",
    "audit:personal:json": "bun run scripts/audit-personal-context.ts --json"
  }
}
```

Antes de editar, leia `package.json` pra ver o shape atual (há convenção de indentação 2 espaços).
</action>
<verify>
  <automated>cd /home/projects/ForgeClaw && bun run audit:personal --out=/tmp/audit-via-script.md && test -s /tmp/audit-via-script.md</automated>
</verify>
<done>`bun run audit:personal` funciona da raiz do monorepo e produz o mesmo relatório que a invocação direta.</done>
</task>

<task id="3" type="auto">
<files>.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/AUDIT-REPORT.md</files>
<action>
Rodar `bun run audit:personal --out=.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/AUDIT-REPORT.md` a partir da raiz `/home/projects/ForgeClaw`.

Depois abrir o arquivo e validar manualmente:
- Contém sumário por severidade (critical/high/medium/low).
- Contém sumário por categoria com ao menos: `personal_name`, `hardcoded_path`, `personal_client`, `private_repo_url`, `playwright_snapshot`.
- A tabela do arquivo `packages/core/src/memory/prompts/janitor.md` aparece.
- A tabela do arquivo `.continue-aqui.md` aparece.
- A entrada `.playwright-mcp/` como `playwright_snapshot` aparece.
- Não há findings falsos em `node_modules/` ou `bun.lock`.

**Não editar manualmente o relatório.** Ele é output determinístico do script. Se estiver errado, corrigir o script e regerar.
</action>
<verify>
  <automated>test -s /home/projects/ForgeClaw/.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/AUDIT-REPORT.md && grep -q 'memory/prompts/janitor.md' /home/projects/ForgeClaw/.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/AUDIT-REPORT.md && grep -q '.continue-aqui.md' /home/projects/ForgeClaw/.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/AUDIT-REPORT.md && grep -q 'playwright_snapshot' /home/projects/ForgeClaw/.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/AUDIT-REPORT.md</automated>
</verify>
<done>AUDIT-REPORT.md existe com findings reais dos arquivos conhecidos (janitor.md, .continue-aqui.md, .playwright-mcp/).</done>
</task>

<task id="4" type="auto">
<files>.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/CLEANUP-CHECKLIST.md</files>
<action>
Derivar manualmente (ou via script helper simples se quiser) o checklist acionável a partir do AUDIT-REPORT.md. Cada item do checklist tem:
- arquivo (path absoluto)
- severidade (a pior do arquivo)
- **ação concreta** (sanitize / delete / replace / move / parametrize)
- **nota** explicando o porquê quando não óbvio

Template literal:

```markdown
# Cleanup Checklist — Contexto Pessoal no Repo ForgeClaw

Derivado de `AUDIT-REPORT.md` em <TIMESTAMP>. Plano 23-02 executa esta lista.

## Legenda de ações
- **sanitize**: reescrever trechos substituindo contexto pessoal por genérico/placeholder
- **delete**: remover o arquivo inteiro do repo
- **replace**: gerar novo arquivo zerado com template genérico
- **move**: mover pra fora do VCS (ou pra gitignore)
- **parametrize**: trocar valor hardcoded por variável de ambiente / config / template var
- **decide**: ação humana — escolher entre opções (ex: skills endpoint)

## Ações críticas (faça primeiro)

- [ ] `packages/core/src/memory/prompts/janitor.md` — **sanitize**
  - remover "do Jonathan" (L3)
  - trocar path `/home/vault/05-pessoal/daily-log/*.md` (L10) → `{{daily_log_dir}}/*.md` ou referência genérica a `$FORGECLAW_DAILY_LOG_DIR`
  - remover exemplos "lfpro, dra nathalia, don vicente" (L41) → exemplos genéricos ("project-a, contact-x, vendor-y")
  - ajustar "destila fatos permanentes" mantendo apenas semântica genérica
- [ ] `packages/core/src/memory/prompts/writer.md` — **sanitize**
  - remover "do Jonathan" (L3, L7)
  - trocar path `/home/vault/05-pessoal/daily-log/YYYY-MM-DD.md` → referência genérica
  - reescrever exemplos (L55, L61, L62, L67, L71, L75, L79, L81, L89) com nomes fictícios: "project-alpha, customer-x, user-y"
- [ ] `packages/core/src/memory-manager.ts` — **sanitize**
  - L34: comentário `// Jonathan is in BRT (UTC-3)` → `// Server runs UTC; dates formatted in the user's configured timezone (default America/Sao_Paulo)`
  - L35-37: manter a lógica, reescrever comentário pra remover "his wall clock"
- [ ] `packages/dashboard/src/lib/mock-data.ts` — **sanitize ou delete**
  - Decidir: manter como fixtures de storybook/dev? Se sim, trocar `vaultPath: "/home/vault"` (L304) → `"/home/example/vault"`; trocar nomes de projeto em `projectDir` (L24, L33, L41, L50, L61, L70) de `/home/projects/ForgeClaw/...` → `/home/example/projects/demo-app/...`; sanitizar `mockMemoryContent` (L222-238) removendo referências ao próprio ForgeClaw e trocando por "demo app"
  - Se este arquivo não é mais usado (Fase 14 tirou mock data como fallback), **delete** e remover imports
- [ ] `.continue-aqui.md` — **delete**
  - É handoff pessoal de sessão com URL de repo privado, bot handle, user id, token mascarado, vault path. Não deve existir no repo.
- [ ] `.playwright-mcp/` (diretório inteiro) — **move** (adicionar ao `.gitignore`) + **delete** dos snapshots comprometidos
  - Adicionar `.playwright-mcp/` ao `.gitignore`
  - `git rm -r --cached .playwright-mcp/` e deletar o diretório do working tree
  - Conteúdo vaza: prompt de sistema ("Você é o **Jonathan Renan Outeiro** — fundador da EcoUp Digital, de Novo Hamburgo/RS"), nomes de projetos internos (gestor-lfpro, bv-otica-crm, inbox-deal-flow), conversas com texto "Oi, Jonathan!"
- [ ] `sessions-tab-initial.png` e `sessions-with-real-names.png` (raiz) — **delete**
  - Screenshots com dados reais. Adicionar `*.png` da raiz ao `.gitignore` ou mover pra `docs/images/` só após sanitizar.
- [ ] `.plano/STATE.md` (L22) — **sanitize**
  - Trocar `github.com/Ecoupdigital/forgeclaw` → `github.com/<org>/forgeclaw` (placeholder) ou apenas `<repo-url>`
- [ ] `README.md` (L48, L102) — **sanitize**
  - Trocar `git clone https://github.com/Ecoupdigital/forgeclaw.git` → `git clone https://github.com/<your-org>/forgeclaw.git` OU o URL público oficial quando existir
- [ ] `ops/forgeclaw.service` — **parametrize**
  - `WorkingDirectory=/home/projects/ForgeClaw` → documentar como placeholder no template do installer ou gerar dinamicamente em `packages/cli/src/utils/service.ts` (já gera? verificar). Remover serviço estático do repo se o installer já cria um dinâmico.
- [ ] `ops/forgeclaw-dashboard.service` — **parametrize** (mesma lógica)
- [ ] `packages/cli/src/commands/install.ts` (L178, L199) — **sanitize**
  - L178 default `/root/projects` → `join(homedir(), 'projects')` (portable)
  - L199 default `/root/obsidian` → remover default (vault path é opcional; só pede se user confirmou que usa Obsidian)

## Ações high (faça em seguida)

- [ ] `packages/core/src/context-builder.ts` (L89) — **parametrize**
  - path `{vaultPath}/05-pessoal/daily-log` assume estrutura do vault do Jonathan. Alternativas:
    a) Tornar configurável via `config.vaultDailyLogRelPath` (default vazio → pula vault daily, usa `~/.forgeclaw/memory/daily`)
    b) Sempre usar `~/.forgeclaw/memory/daily` e documentar que `vaultPath` é só pointer de contexto, não fonte de daily log
  - **decide** qual caminho. BRIEFING-M.md (M1) já apontou isto como gap.
- [ ] `packages/dashboard/src/app/api/skills/route.ts` — **decide**
  - Aponta pra `~/.claude/skills/` — que é do runtime do Claude Code, não do ForgeClaw. Decidir:
    a) Manter (produto assume user tem Claude Code com skills — provável)
    b) Adicionar fallback `~/.forgeclaw/skills/` e UI muda pra preferir local
  - Se manter, adicionar comment no código explicando o acoplamento com Claude Code CLI.

## Ações medium

- [ ] `.plano/BRIEFING-M.md` — **decide**
  - É histórico (já resolvido pelas Fases 18+). Manter ou mover pra `.plano/archive/`. Não é regressão de produto, mas vaza `/home/vault` nos arquivos citados.
- [ ] `.plano/fases/18-core-hardening/*` — **no-op**
  - Menciona `/home/vault` em contexto histórico. Não distribui. Plano 23-03 deve whitelist `.plano/` na guard de CI.
- [ ] `CHANGELOG.md` — **no-op** (descrição de features, sem PII).

## Ações informativas (verificar, não necessariamente agir)

- [ ] Conferir `packages/core/src/state-store.ts` — varredura linhas de schema não achou PII; escopo sensível é só o banco runtime (`~/.forgeclaw/db/forgeclaw.db`) que não está no repo e já está em `.gitignore` implícito (fora do repo).
- [ ] Conferir se `~/.forgeclaw/HEARTBEAT.md` tem cópia commitada no repo como exemplo. Se sim, revisar.
```

Critério: ordenar do mais crítico pro menos crítico. **Toda entrada precisa ter ação explícita** (não deixar "ver o que fazer").
</action>
<verify>
  <automated>test -s /home/projects/ForgeClaw/.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/CLEANUP-CHECKLIST.md && grep -q 'sanitize' /home/projects/ForgeClaw/.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/CLEANUP-CHECKLIST.md && grep -q 'delete' /home/projects/ForgeClaw/.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/CLEANUP-CHECKLIST.md && grep -q '.playwright-mcp' /home/projects/ForgeClaw/.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/CLEANUP-CHECKLIST.md && grep -q 'janitor.md' /home/projects/ForgeClaw/.plano/fases/23-auditoria-de-despersonaliza-o-mapear-e-expurgar-contexto-pessoal-do-repo-harness-heartbeat-mem-ria-skills-antes-de-distribuir/CLEANUP-CHECKLIST.md</automated>
</verify>
<done>CLEANUP-CHECKLIST.md existe com ações categorizadas (critical/high/medium), cobre todos os arquivos conhecidos e cada item tem verbo acionável (sanitize/delete/replace/parametrize/decide).</done>
</task>

<task id="5" type="auto">
<files>scripts/audit-personal-context.ts</files>
<action>
Validação final de qualidade do scanner: rodar uma segunda passada confirmando que findings **esperados** aparecem no relatório. Este task é de checagem — não escreve nada novo, só valida.

Esperados (no mínimo):
- `packages/core/src/memory/prompts/janitor.md` → personal_name (L3), hardcoded_path (L10 /home/vault), personal_client (L41 lfpro)
- `packages/core/src/memory/prompts/writer.md` → personal_name (L3), hardcoded_path (L7), personal_client (L55, 61, 62 gestor-lfpro)
- `packages/core/src/memory-manager.ts` → personal_name (L34 Jonathan)
- `packages/dashboard/src/lib/mock-data.ts` → hardcoded_path (/home/vault L304)
- `.continue-aqui.md` → private_repo_url (L4 Ecoupdigital), personal_handle (ForgeClawUP_bot), personal_userid (450030767), hardcoded_path (/home/vault, /home/projects), bot_token_fragment (8662287719:)
- `.playwright-mcp/` → playwright_snapshot (1 entry)
- `README.md` → private_repo_url (L48, L102), hardcoded_path já não tem (/home/usuario/obsidian é placeholder)
- `ops/forgeclaw.service`, `ops/forgeclaw-dashboard.service` → hardcoded_path (L8 /home/projects/ForgeClaw)
- `packages/cli/src/commands/install.ts` → hardcoded_path (/root/projects L178, /root/obsidian L199)
- `.plano/STATE.md` → private_repo_url (L22)

Se algum esperado **não aparecer**, adicionar regra ao script em task#1 e regerar. Se achados falsos críticos (ex: um string legítima tipo `/home/projects/ForgeClaw` em código de build que DEVE ficar), não remover regra — o plano 23-02 decide por arquivo.

Comando pra auditar coverage:

```bash
cd /home/projects/ForgeClaw
bun run audit:personal:json --out=/tmp/audit.json
jq -r '.[] | "\(.file):\(.line):\(.category)"' /tmp/audit.json | sort -u > /tmp/covered.txt
cat /tmp/covered.txt | head -50
# Verificação manual: arquivos esperados estão listados?
for f in packages/core/src/memory/prompts/janitor.md packages/core/src/memory/prompts/writer.md packages/core/src/memory-manager.ts packages/dashboard/src/lib/mock-data.ts .continue-aqui.md README.md .plano/STATE.md packages/cli/src/commands/install.ts ops/forgeclaw.service ops/forgeclaw-dashboard.service; do
  if grep -q "^$f:" /tmp/covered.txt; then echo "OK: $f"; else echo "MISS: $f"; fi
done
```

Toda linha "MISS" é um bug no scanner.
</action>
<verify>
  <automated>cd /home/projects/ForgeClaw && bun run audit:personal:json --out=/tmp/audit.json && jq -r '.[] | "\(.file):\(.line):\(.category)"' /tmp/audit.json | sort -u > /tmp/covered.txt && for f in packages/core/src/memory/prompts/janitor.md packages/core/src/memory/prompts/writer.md packages/core/src/memory-manager.ts packages/dashboard/src/lib/mock-data.ts .continue-aqui.md README.md .plano/STATE.md packages/cli/src/commands/install.ts ops/forgeclaw.service ops/forgeclaw-dashboard.service; do grep -q "^$f:" /tmp/covered.txt || { echo "MISS: $f"; exit 1; }; done</automated>
</verify>
<done>Todos os 10 arquivos-alvo conhecidos aparecem no relatório (zero linhas "MISS" no loop de verificação).</done>
</task>

## Criterios de Sucesso

- [ ] `bun run audit:personal` executável da raiz do monorepo sem erro.
- [ ] `AUDIT-REPORT.md` gerado com sumário por severidade + categoria + tabela por arquivo.
- [ ] Todos os 10 arquivos-alvo conhecidos aparecem no relatório (validado pelo loop da tarefa 5).
- [ ] `CLEANUP-CHECKLIST.md` existe com ações acionáveis (sanitize/delete/replace/parametrize/decide) por arquivo.
- [ ] `.playwright-mcp/` registrado como `playwright_snapshot` critical.
- [ ] Scanner não tem dependências externas novas (só `node:fs/promises`, `node:path`).
- [ ] Zero findings em `node_modules/`, `bun.lock`, `.git/` (validação: `grep -q 'node_modules' AUDIT-REPORT.md` deve falhar).
