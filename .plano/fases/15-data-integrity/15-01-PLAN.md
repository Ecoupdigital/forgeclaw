---
phase: 15-data-integrity
plan: 15-01
type: fix
autonomous: true
wave: 0
depends_on: []
requirements: [HIG-H2, HIG-H3, HIG-H4]
must_haves:
  truths:
    - "Cron execution produces exactly 1 log row per run, not 2"
    - "Session keys always use chatId:topicId format, with 0 for DMs"
    - "writeConfig() never persists masked tokens back to disk"
  artifacts:
    - path: "packages/core/src/state-store.ts"
      provides: "updateCronLog() method for updating existing log rows"
    - path: "packages/core/src/cron-engine.ts"
      provides: "Uses updateCronLog instead of second createCronLog"
    - path: "packages/core/src/session-manager.ts"
      provides: "buildKey always returns chatId:topicId format"
    - path: "packages/core/src/ws-server.ts"
      provides: "parseSessionKey handles topicId=0 as null consistently"
    - path: "packages/dashboard/src/lib/core.ts"
      provides: "writeConfig strips all masked fields, not just botToken"
  key_links:
    - from: "cron-engine.ts"
      to: "state-store.ts"
      via: "updateCronLog(logId, { finishedAt, status, output })"
    - from: "session-manager.ts"
      to: "ws-server.ts"
      via: "consistent chatId:topicId key format"
---

# Fase 15 Plano 01: Data Integrity Fixes (H2 + H3 + H4)

**Objetivo:** Corrigir tres bugs de integridade de dados: log duplicado de cron, mismatch de session key entre bot e dashboard, e corrupcao de tokens mascarados ao salvar config.

## Contexto

@packages/core/src/state-store.ts -- StateStore com createCronLog mas sem updateCronLog
@packages/core/src/cron-engine.ts -- executeJob() linhas 350-414, cria 2 log entries
@packages/core/src/session-manager.ts -- buildKey() retorna formato inconsistente
@packages/core/src/ws-server.ts -- parseSessionKey() assume formato com colon
@packages/dashboard/src/lib/core.ts -- writeConfig() so trata botToken mascarado, ignora dashboardToken

## Tarefas

<task id="1" type="auto">
<files>packages/core/src/state-store.ts</files>
<action>
Adicionar metodo `updateCronLog` na classe `StateStore`, logo apos o metodo `createCronLog` (apos linha 517).

O metodo deve ter esta assinatura e implementacao exata:

```typescript
updateCronLog(id: number, updates: Partial<Pick<CronLog, 'finishedAt' | 'status' | 'output'>>): void {
  const fields: string[] = [];
  const values: (string | number | null)[] = [];

  if (updates.finishedAt !== undefined) {
    fields.push('finished_at = ?');
    values.push(updates.finishedAt);
  }
  if (updates.status !== undefined) {
    fields.push('status = ?');
    values.push(updates.status);
  }
  if (updates.output !== undefined) {
    fields.push('output = ?');
    values.push(updates.output);
  }

  if (fields.length === 0) return;
  values.push(id);
  this.db.run(`UPDATE cron_logs SET ${fields.join(', ')} WHERE id = ?`, values);
}
```

Importacoes necessarias: nenhuma nova -- `CronLog` ja esta importado no topo do arquivo via `import type { ..., CronLog, ... } from './types'`.

NAO alterar nenhum outro metodo nesta tarefa.
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -n "updateCronLog" packages/core/src/state-store.ts | head -5</automated></verify>
<done>Metodo `updateCronLog(id, updates)` existe na classe StateStore com campos finishedAt, status, output.</done>
</task>

<task id="2" type="auto">
<files>packages/core/src/cron-engine.ts</files>
<action>
No metodo `executeJob()`, substituir o segundo `stateStore.createCronLog(...)` (linhas 408-414) por uma chamada ao novo `updateCronLog`.

ANTES (linhas 405-414):
```typescript
// Update cron log
// Note: We create a new log entry for the final state since SQLite update by log id
// would require an updateCronLog method. We use the existing log entry approach.
stateStore.createCronLog({
  jobId: job.id,
  startedAt,
  finishedAt,
  status,
  output: output.slice(0, 10_000), // Limit output size
});
```

DEPOIS:
```typescript
// Update the "running" log entry with final state
stateStore.updateCronLog(logId, {
  finishedAt,
  status,
  output: output.slice(0, 10_000), // Limit output size
});
```

A variavel `logId` ja existe -- e o retorno de `stateStore.createCronLog(...)` na linha 350.
A variavel `stateStore` ja esta importada no escopo.

Importacoes necessarias: nenhuma nova.

NAO alterar a primeira chamada `createCronLog` (linha 350) -- ela deve permanecer criando a entry "running".
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -n "createCronLog\|updateCronLog" packages/core/src/cron-engine.ts</automated></verify>
<done>cron-engine.ts tem exatamente 1 createCronLog (o "running" inicial) e 1 updateCronLog (a finalizacao). Nenhum segundo createCronLog.</done>
</task>

<task id="3" type="auto">
<files>packages/core/src/session-manager.ts</files>
<action>
Alterar o metodo `buildKey` na classe `SessionManager` (linha 8-9).

ANTES:
```typescript
private buildKey(chatId: number, topicId: number | null): string {
  return topicId ? `${chatId}:${topicId}` : `${chatId}`;
}
```

DEPOIS:
```typescript
private buildKey(chatId: number, topicId: number | null): string {
  return `${chatId}:${topicId ?? 0}`;
}
```

Isso garante que DMs (topicId=null) geram key "12345:0" ao inves de "12345", alinhando com o formato que o dashboard espera.

Importacoes necessarias: nenhuma nova.
NAO alterar nenhum outro metodo.
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -A2 "buildKey" packages/core/src/session-manager.ts</automated></verify>
<done>buildKey sempre retorna formato "chatId:topicId", usando 0 quando topicId e null.</done>
</task>

<task id="4" type="auto">
<files>packages/core/src/ws-server.ts</files>
<action>
Verificar que `parseSessionKey()` (linha 125-131) ja trata o formato corretamente.

Codigo atual:
```typescript
function parseSessionKey(key: string): { chatId: number; topicId: number | null } {
  const parts = key.split(':');
  const chatId = parseInt(parts[0], 10);
  const topicId = parts.length > 1 ? parseInt(parts[1], 10) : null;
  // topicId 0 means no topic
  return { chatId, topicId: topicId === 0 ? null : topicId };
}
```

Este codigo JA funciona corretamente:
- "12345:0" -> parts = ["12345", "0"] -> topicId = 0 -> retorna null (correto)
- "12345:999" -> parts = ["12345", "999"] -> topicId = 999 (correto)
- "12345" (formato antigo, legacy) -> parts = ["12345"] -> topicId = null (correto, backward compat)

NAO alterar este arquivo. A funcao ja e consistente com o novo formato de buildKey.

Porem, verificar se `handleSend` passa topicId corretamente para `stateStore.createMessage`. Na linha 161, vemos:
```typescript
topicId: topicId ?? chatId,
```
Isso significa: se topicId e null (DM), usa chatId como topic_id na tabela messages. Esse comportamento e pre-existente e nao faz parte deste fix -- a session key e o ponto de mismatch, nao o message storage.

Se tudo estiver correto, nenhuma alteracao neste arquivo.
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -n "parseSessionKey" packages/core/src/ws-server.ts</automated></verify>
<done>parseSessionKey ja converte topicId=0 para null, compativel com o novo formato "chatId:0" gerado por buildKey.</done>
</task>

<task id="5" type="auto">
<files>packages/dashboard/src/lib/core.ts</files>
<action>
Alterar a funcao `writeConfig()` (linhas 865-893) para tratar TODOS os campos mascarados, nao apenas botToken.

ANTES (linhas 879-885):
```typescript
// If the incoming botToken looks masked, preserve original
const incoming = config as Record<string, unknown>;
if (
  typeof incoming.botToken === "string" &&
  incoming.botToken.includes("***hidden***")
) {
  incoming.botToken = existing.botToken;
}
```

DEPOIS:
```typescript
// Strip ANY field that contains the mask pattern — prevents masked
// tokens (botToken, dashboardToken, or future secrets) from being
// persisted back to disk.
const MASK = "***hidden***";
const incoming = { ...config };
for (const key of Object.keys(incoming)) {
  if (typeof incoming[key] === "string" && (incoming[key] as string).includes(MASK)) {
    // Preserve original value from disk, or delete entirely if no original exists
    if (key in existing) {
      incoming[key] = existing[key];
    } else {
      delete incoming[key];
    }
  }
}
```

Mudancas-chave:
1. Usa spread `{ ...config }` para nao mutar o objeto original passado por referencia
2. Itera TODOS os campos, nao so botToken -- captura dashboardToken e qualquer token futuro
3. Se o campo mascarado nao existia no arquivo original (edge case), deleta ao inves de escrever lixo

A linha final `await writeFile(CONFIG_PATH, JSON.stringify(incoming, null, 2), "utf-8")` permanece inalterada -- `incoming` agora e a copia limpa.

Importacoes necessarias: nenhuma nova.
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -n "hidden\|MASK\|masked" packages/dashboard/src/lib/core.ts</automated></verify>
<done>writeConfig() itera todos os campos do incoming config e preserva valor original para qualquer campo contendo "***hidden***". Tanto botToken quanto dashboardToken (e futuros secrets) sao protegidos.</done>
</task>

<task id="6" type="auto">
<files>packages/core/src/index.ts</files>
<action>
Verificar se `updateCronLog` precisa ser exportado no barrel export do pacote core. Abrir `packages/core/src/index.ts` e verificar se `stateStore` ja e re-exportado.

Se `stateStore` ja e exportado como instancia (ex: `export { stateStore } from './state-store'`), entao `updateCronLog` ja esta acessivel via `stateStore.updateCronLog()` e nenhuma alteracao e necessaria.

Se o barrel nao exporta stateStore, verificar se cron-engine importa diretamente de `./state-store` (ele faz: `import { stateStore } from './state-store'` no topo do arquivo). Nesse caso, tambem nao precisa de alteracao no barrel.

Resultado esperado: NENHUMA alteracao necessaria neste arquivo. Esta tarefa e apenas verificacao.
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep "stateStore\|state-store" packages/core/src/index.ts packages/core/src/cron-engine.ts | head -10</automated></verify>
<done>cron-engine importa stateStore diretamente; updateCronLog e acessivel sem mudancas no barrel.</done>
</task>

<task id="7" type="checkpoint:human-verify">
<files>packages/core/src/cron-engine.ts, packages/core/src/session-manager.ts, packages/dashboard/src/lib/core.ts</files>
<action>
Verificacao manual pos-implementacao:

1. **H2 (cron log duplicado):** No servidor, rodar um cron job manualmente (via dashboard "Run Now" ou aguardar schedule). Apos execucao, checar no SQLite:
   ```sql
   SELECT id, job_id, status, finished_at, output FROM cron_logs ORDER BY id DESC LIMIT 5;
   ```
   Deve haver UMA entry por execucao, com status 'success' ou 'failed', finished_at preenchido, e output nao-null.
   NAO deve haver entry com status 'running' e finished_at NULL (orfas).

2. **H3 (session key):** Enviar uma DM ao bot no Telegram (nao em forum topic). Checar a session criada:
   ```sql
   SELECT id FROM sessions ORDER BY created_at DESC LIMIT 5;
   ```
   O id deve estar no formato "CHATID:0", nao apenas "CHATID".

3. **H4 (config write):** No dashboard, abrir Config tab, nao alterar tokens (que aparecem mascarados), mudar qualquer outro campo (ex: workingDir), salvar. Verificar no servidor:
   ```bash
   cat ~/.forgeclaw/forgeclaw.config.json | jq '.botToken, .dashboardToken'
   ```
   Ambos tokens devem conter o valor REAL, nao "abc123***hidden***".
</action>
<verify><automated>echo "Manual verification required"</automated></verify>
<done>Os tres fixes funcionam em producao: cron logs sem duplicatas, session keys consistentes, tokens nunca corrompidos.</done>
</task>

## Criterios de Sucesso

- [ ] `stateStore.updateCronLog()` existe e atualiza rows existentes no cron_logs
- [ ] `cron-engine.ts` tem exatamente 1 `createCronLog` + 1 `updateCronLog` (nao 2 creates)
- [ ] `sessionManager.buildKey()` sempre retorna formato "chatId:topicId" (com 0 para null)
- [ ] `parseSessionKey()` continua convertendo topicId=0 para null (ja era assim)
- [ ] `writeConfig()` filtra TODOS os campos mascarados, nao so botToken
- [ ] Nenhum import novo necessario em nenhum arquivo
