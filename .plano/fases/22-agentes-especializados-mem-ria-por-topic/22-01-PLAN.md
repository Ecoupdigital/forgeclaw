---
phase: 22-agentes-especializados-mem-ria-por-topic
plan: 22-01
type: feature
autonomous: true
wave: 0
depends_on: []
requirements: [AGT-01, AGT-02]
must_haves:
  truths:
    - "Tabela agents existe no SQLite com schema correto"
    - "Tabela topics tem coluna agent_id como FK para agents"
    - "Tipos AgentConfig e TopicInfo atualizado existem em core e dashboard"
    - "StateStore tem CRUD completo para agents"
    - "core.ts (dashboard) tem CRUD completo para agents via better-sqlite3"
  artifacts:
    - path: "packages/core/src/types.ts"
      provides: "Interface AgentConfig"
    - path: "packages/core/src/state-store.ts"
      provides: "Migration agents + topics.agent_id + CRUD methods"
    - path: "packages/dashboard/src/lib/types.ts"
      provides: "Interface AgentConfig no dashboard"
    - path: "packages/dashboard/src/lib/core.ts"
      provides: "CRUD de agents via better-sqlite3"
  key_links:
    - from: "types.ts (core)"
      to: "state-store.ts"
      via: "AgentConfig interface usada nos metodos CRUD"
    - from: "types.ts (dashboard)"
      to: "core.ts (dashboard)"
      via: "AgentConfig interface usada nos retornos"
---

# Fase 22 Plano 01: Schema + Tipos + Data Access Layer

**Objetivo:** Criar a tabela `agents` no SQLite, adicionar FK `agent_id` na tabela `topics`, definir tipos TypeScript em core e dashboard, e implementar CRUD completo de agents em ambos state-store (Bun) e core.ts (better-sqlite3).

## Contexto

@packages/core/src/types.ts -- tipos existentes, TopicInfo ja tem runtime/runtimeFallback
@packages/core/src/state-store.ts -- migration pattern (PRAGMA table_info gate), CRUD pattern
@packages/dashboard/src/lib/types.ts -- tipos dashboard, espelho do core
@packages/dashboard/src/lib/core.ts -- acesso SQLite via better-sqlite3, pattern de mappers

## Tarefas

<task id="1" type="auto">
<files>packages/core/src/types.ts</files>
<action>
Adicionar a interface `AgentConfig` e o tipo `MemoryMode` ao arquivo de tipos do core.
Adicionar tambem `agentId` ao `TopicInfo`.

Inserir APOS a interface `TopicInfo` existente (linha ~80):

```typescript
export type MemoryMode = 'global' | 'filtered';

export interface AgentConfig {
  id: number;
  name: string;
  systemPrompt: string | null;
  memoryMode: MemoryMode;
  memoryDomainFilter: string[];  // parsed from JSON, e.g. ["financeiro", "clientes"]
  defaultRuntime: RuntimeName | null;
  createdAt: number;
  updatedAt: number;
}
```

Na interface `TopicInfo` existente, adicionar o campo:
```typescript
  /** FK to agents table. NULL = no agent linked. */
  agentId?: number | null;
```

Esse campo deve ser adicionado APOS o campo `runtimeFallback`.
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -c 'AgentConfig' packages/core/src/types.ts && grep -c 'agentId' packages/core/src/types.ts && grep -c 'MemoryMode' packages/core/src/types.ts</automated></verify>
<done>Interface AgentConfig com 8 campos tipados existe em types.ts. TopicInfo tem campo agentId. Tipo MemoryMode exportado.</done>
</task>

<task id="2" type="auto">
<files>packages/core/src/state-store.ts</files>
<action>
Adicionar migration idempotente para a tabela `agents` e coluna `agent_id` em `topics`.

**1) Na secao de Mission Control tables (apos o bloco try que cria webhooks), adicionar um novo bloco try:**

```typescript
// Agents: specialized agents linked to topics
try {
  this.db.exec(`
    CREATE TABLE IF NOT EXISTS agents (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      name TEXT NOT NULL,
      system_prompt TEXT,
      memory_mode TEXT NOT NULL DEFAULT 'global',
      memory_domain_filter TEXT,
      default_runtime TEXT,
      created_at INTEGER NOT NULL,
      updated_at INTEGER NOT NULL
    );
  `);

  // Migration: add agent_id FK to topics
  const topicCols = (this.db.query("PRAGMA table_info(topics)").all() as Array<{ name: string }>).map(c => c.name);
  if (!topicCols.includes('agent_id')) {
    this.db.exec('ALTER TABLE topics ADD COLUMN agent_id INTEGER REFERENCES agents(id)');
  }

  console.log('[state-store] Agents tables ready');
} catch (err) {
  console.warn('[state-store] Agents migration failed:', err);
}
```

**2) Atualizar TopicRow (interface privada, ~linha 1199) para incluir:**
```typescript
agent_id: number | null;
```

**3) Atualizar mapTopicRow para incluir:**
```typescript
agentId: row.agent_id ?? null,
```

**4) Atualizar TODAS as queries SELECT de topics (getTopic, getTopicByChatAndThread, listTopics) para incluir `agent_id` na lista de colunas. Sao 5 ocorrencias no total (3 metodos + 2 variantes do getTopicByChatAndThread).**

**5) Adicionar AgentRow interface e mapper:**

```typescript
interface AgentRow {
  id: number;
  name: string;
  system_prompt: string | null;
  memory_mode: string;
  memory_domain_filter: string | null;
  default_runtime: string | null;
  created_at: number;
  updated_at: number;
}

function mapAgentRow(row: AgentRow): import('./types').AgentConfig {
  return {
    id: row.id,
    name: row.name,
    systemPrompt: row.system_prompt,
    memoryMode: (row.memory_mode as 'global' | 'filtered') ?? 'global',
    memoryDomainFilter: row.memory_domain_filter ? JSON.parse(row.memory_domain_filter) as string[] : [],
    defaultRuntime: (row.default_runtime as 'claude-code' | 'codex' | null) ?? null,
    createdAt: row.created_at,
    updatedAt: row.updated_at,
  };
}
```

**6) Adicionar metodos CRUD de agents na classe StateStore:**

```typescript
// ---------------- Agents ----------------

createAgent(agent: Omit<import('./types').AgentConfig, 'id'>): number {
  const result = this.db.run(
    `INSERT INTO agents (name, system_prompt, memory_mode, memory_domain_filter, default_runtime, created_at, updated_at)
     VALUES (?, ?, ?, ?, ?, ?, ?)`,
    [agent.name, agent.systemPrompt, agent.memoryMode, agent.memoryDomainFilter.length > 0 ? JSON.stringify(agent.memoryDomainFilter) : null, agent.defaultRuntime, agent.createdAt, agent.updatedAt]
  );
  return Number(result.lastInsertRowid);
}

getAgent(id: number): import('./types').AgentConfig | null {
  const row = this.db.query(
    'SELECT id, name, system_prompt, memory_mode, memory_domain_filter, default_runtime, created_at, updated_at FROM agents WHERE id = ?'
  ).get(id) as AgentRow | null;
  return row ? mapAgentRow(row) : null;
}

listAgents(): import('./types').AgentConfig[] {
  const rows = this.db.query(
    'SELECT id, name, system_prompt, memory_mode, memory_domain_filter, default_runtime, created_at, updated_at FROM agents ORDER BY name ASC'
  ).all() as AgentRow[];
  return rows.map(mapAgentRow);
}

updateAgent(id: number, updates: Partial<Omit<import('./types').AgentConfig, 'id' | 'createdAt'>>): void {
  const fields: string[] = [];
  const values: (string | number | null)[] = [];
  if (updates.name !== undefined) { fields.push('name = ?'); values.push(updates.name); }
  if (updates.systemPrompt !== undefined) { fields.push('system_prompt = ?'); values.push(updates.systemPrompt); }
  if (updates.memoryMode !== undefined) { fields.push('memory_mode = ?'); values.push(updates.memoryMode); }
  if (updates.memoryDomainFilter !== undefined) {
    fields.push('memory_domain_filter = ?');
    values.push(updates.memoryDomainFilter.length > 0 ? JSON.stringify(updates.memoryDomainFilter) : null);
  }
  if (updates.defaultRuntime !== undefined) { fields.push('default_runtime = ?'); values.push(updates.defaultRuntime); }
  if (fields.length === 0) return;
  fields.push('updated_at = ?');
  values.push(Date.now());
  values.push(id);
  this.db.run(`UPDATE agents SET ${fields.join(', ')} WHERE id = ?`, values);
}

deleteAgent(id: number): void {
  // Unlink topics first (set agent_id = NULL)
  this.db.run('UPDATE topics SET agent_id = NULL WHERE agent_id = ?', [id]);
  this.db.run('DELETE FROM agents WHERE id = ?', [id]);
}

// Update topic's agent_id
updateTopicAgent(id: number, agentId: number | null): void {
  this.db.run('UPDATE topics SET agent_id = ? WHERE id = ?', [agentId, id]);
}
```

**Importar AgentConfig no import do topo:**
Nao precisa -- os metodos usam `import('./types').AgentConfig` inline, seguindo o pattern existente no arquivo (ex: TokenUsage, Activity, Webhook).
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -c 'createAgent' packages/core/src/state-store.ts && grep -c 'agent_id' packages/core/src/state-store.ts && grep -c 'deleteAgent' packages/core/src/state-store.ts</automated></verify>
<done>Tabela agents criada via migration idempotente. Coluna agent_id adicionada a topics. CRUD completo (create, get, list, update, delete) + updateTopicAgent. TopicRow e mapTopicRow incluem agent_id.</done>
</task>

<task id="3" type="auto">
<files>packages/dashboard/src/lib/types.ts</files>
<action>
Adicionar tipos para agents no dashboard. Inserir APOS a interface `TopicInfo`:

```typescript
export type MemoryMode = "global" | "filtered";

export interface AgentConfig {
  id: number;
  name: string;
  systemPrompt: string | null;
  memoryMode: MemoryMode;
  memoryDomainFilter: string[];
  defaultRuntime: RuntimeName | null;
  createdAt: number;
  updatedAt: number;
}
```

Na interface `TopicInfo` existente, adicionar:
```typescript
  agentId?: number | null;
```

Apos o campo `runtimeFallback`.
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -c 'AgentConfig' packages/dashboard/src/lib/types.ts && grep -c 'agentId' packages/dashboard/src/lib/types.ts</automated></verify>
<done>Interface AgentConfig e MemoryMode existem em dashboard types. TopicInfo tem agentId.</done>
</task>

<task id="4" type="auto">
<files>packages/dashboard/src/lib/core.ts</files>
<action>
Adicionar CRUD de agents e atualizar topic mappers no core.ts do dashboard.

**1) Adicionar import de AgentConfig no import de types (topo, linha ~27):**
Adicionar `AgentConfig` ao import de `./types`.

**2) Adicionar AgentRow interface (junto com as outras Row interfaces, ~linha 125):**

```typescript
interface AgentRow {
  id: number;
  name: string;
  system_prompt: string | null;
  memory_mode: string;
  memory_domain_filter: string | null;
  default_runtime: string | null;
  created_at: number;
  updated_at: number;
}
```

**3) Adicionar mapAgent function:**

```typescript
function mapAgent(row: AgentRow): AgentConfig {
  return {
    id: row.id,
    name: row.name,
    systemPrompt: row.system_prompt,
    memoryMode: (row.memory_mode as "global" | "filtered") ?? "global",
    memoryDomainFilter: row.memory_domain_filter ? JSON.parse(row.memory_domain_filter) as string[] : [],
    defaultRuntime: (row.default_runtime as "claude-code" | "codex" | null) ?? null,
    createdAt: row.created_at,
    updatedAt: row.updated_at,
  };
}
```

**4) Atualizar TopicRow para incluir `agent_id: number | null;`**

**5) Atualizar mapTopic para incluir `agentId: row.agent_id ?? null,`**

**6) Atualizar TODAS as queries SELECT de topics (listTopics, getTopic) para incluir `agent_id` na lista de colunas.**

**7) Adicionar funcoes CRUD para agents (apos a secao de Topics, antes de Cron Jobs):**

```typescript
// --- Public API: Agents ---

export function listAgents(): AgentConfig[] | null {
  const d = getDb();
  if (!d) return null;
  try {
    const rows = d
      .prepare(
        "SELECT id, name, system_prompt, memory_mode, memory_domain_filter, default_runtime, created_at, updated_at FROM agents ORDER BY name ASC"
      )
      .all() as AgentRow[];
    return rows.map(mapAgent);
  } catch {
    return null;
  }
}

export function getAgent(id: number): AgentConfig | null {
  const d = getDb();
  if (!d) return null;
  try {
    const row = d
      .prepare(
        "SELECT id, name, system_prompt, memory_mode, memory_domain_filter, default_runtime, created_at, updated_at FROM agents WHERE id = ?"
      )
      .get(id) as AgentRow | undefined;
    return row ? mapAgent(row) : null;
  } catch {
    return null;
  }
}

export function createAgent(agent: Omit<AgentConfig, "id">): number | null {
  const d = getDb();
  if (!d) return null;
  try {
    const result = d
      .prepare(
        "INSERT INTO agents (name, system_prompt, memory_mode, memory_domain_filter, default_runtime, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?)"
      )
      .run(
        agent.name,
        agent.systemPrompt,
        agent.memoryMode,
        agent.memoryDomainFilter.length > 0 ? JSON.stringify(agent.memoryDomainFilter) : null,
        agent.defaultRuntime,
        agent.createdAt,
        agent.updatedAt
      );
    return Number(result.lastInsertRowid);
  } catch {
    return null;
  }
}

export function updateAgent(
  id: number,
  updates: Partial<Omit<AgentConfig, "id" | "createdAt">>
): boolean {
  const d = getDb();
  if (!d) return false;
  try {
    const fields: string[] = [];
    const values: (string | number | null)[] = [];
    if (updates.name !== undefined) { fields.push("name = ?"); values.push(updates.name); }
    if (updates.systemPrompt !== undefined) { fields.push("system_prompt = ?"); values.push(updates.systemPrompt); }
    if (updates.memoryMode !== undefined) { fields.push("memory_mode = ?"); values.push(updates.memoryMode); }
    if (updates.memoryDomainFilter !== undefined) {
      fields.push("memory_domain_filter = ?");
      values.push(updates.memoryDomainFilter.length > 0 ? JSON.stringify(updates.memoryDomainFilter) : null);
    }
    if (updates.defaultRuntime !== undefined) { fields.push("default_runtime = ?"); values.push(updates.defaultRuntime); }
    if (fields.length === 0) return true;
    fields.push("updated_at = ?");
    values.push(Date.now());
    values.push(id);
    d.prepare(`UPDATE agents SET ${fields.join(", ")} WHERE id = ?`).run(...values);
    return true;
  } catch {
    return false;
  }
}

export function deleteAgent(id: number): boolean {
  const d = getDb();
  if (!d) return false;
  try {
    // Unlink topics first
    d.prepare("UPDATE topics SET agent_id = NULL WHERE agent_id = ?").run(id);
    d.prepare("DELETE FROM agents WHERE id = ?").run(id);
    return true;
  } catch {
    return false;
  }
}

export function updateTopicAgent(topicId: number, agentId: number | null): boolean {
  const d = getDb();
  if (!d) return false;
  try {
    d.prepare("UPDATE topics SET agent_id = ? WHERE id = ?").run(agentId, topicId);
    return true;
  } catch {
    return false;
  }
}

/**
 * List topics linked to a specific agent.
 */
export function listTopicsByAgent(agentId: number): TopicInfo[] | null {
  const d = getDb();
  if (!d) return null;
  try {
    const rows = d
      .prepare(
        "SELECT id, thread_id, chat_id, name, project_dir, session_id, created_at, runtime, runtime_fallback, agent_id FROM topics WHERE agent_id = ? ORDER BY created_at DESC"
      )
      .all(agentId) as TopicRow[];
    return rows.map(mapTopic);
  } catch {
    return null;
  }
}
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -c 'listAgents' packages/dashboard/src/lib/core.ts && grep -c 'createAgent' packages/dashboard/src/lib/core.ts && grep -c 'agent_id' packages/dashboard/src/lib/core.ts</automated></verify>
<done>CRUD completo de agents no dashboard core.ts. TopicRow e mapTopic incluem agent_id. listTopicsByAgent disponivel.</done>
</task>

<task id="5" type="auto">
<files>packages/dashboard/src/app/api/topics/route.ts</files>
<action>
Atualizar a rota GET /api/topics para incluir `agentId` no retorno slim.

Na linha que monta o `slim` array, adicionar `agentId: t.agentId ?? null` ao objeto mapeado:

```typescript
const slim = topics.map((t) => ({
  id: t.id,
  name: t.name ?? `Topic #${t.id}`,
  chatId: t.chatId,
  threadId: t.threadId,
  runtime: t.runtime ?? null,
  runtimeFallback: t.runtimeFallback ?? false,
  agentId: t.agentId ?? null,
}));
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -c 'agentId' packages/dashboard/src/app/api/topics/route.ts</automated></verify>
<done>GET /api/topics retorna agentId no payload de cada topic.</done>
</task>

## Criterios de Sucesso

- [ ] Tabela `agents` criada com schema correto (id, name, system_prompt, memory_mode, memory_domain_filter, default_runtime, created_at, updated_at)
- [ ] Coluna `agent_id` adicionada a `topics` como FK para `agents`
- [ ] Interface `AgentConfig` existe em core e dashboard types
- [ ] `TopicInfo` inclui campo `agentId` em core e dashboard
- [ ] StateStore tem createAgent, getAgent, listAgents, updateAgent, deleteAgent, updateTopicAgent
- [ ] core.ts (dashboard) tem os mesmos metodos via better-sqlite3
- [ ] GET /api/topics retorna agentId
