---
phase: 22-agentes-especializados-mem-ria-por-topic
plan: 22-03
type: feature
autonomous: true
wave: 1
depends_on: [22-01]
requirements: [AGT-06, AGT-07, AGT-08]
must_haves:
  truths:
    - "ContextBuilder aceita AgentConfig opcional e prependa system_prompt ao contexto"
    - "ContextBuilder passa entityFilter ao memoryManagerV2.prefetchAll quando agent tem memory_mode='filtered'"
    - "text.ts carrega agent via topicRow.agentId e passa ao ContextBuilder"
    - "ws-server.ts carrega agent via topicRow.agentId e passa ao ContextBuilder"
    - "prefetchAll do MemoryManager aceita entityFilter opcional"
    - "BuiltinMemoryProvider filtra por entity_name via memory_refs quando entityFilter presente"
  artifacts:
    - path: "packages/core/src/context-builder.ts"
      provides: "build() aceita AgentConfig, prependa systemPrompt, filtra memoria"
    - path: "packages/core/src/memory/manager.ts"
      provides: "prefetchAll aceita entityFilter opcional"
    - path: "packages/core/src/memory/builtin-provider.ts"
      provides: "prefetch filtra por entity_name via memory_refs"
    - path: "packages/core/src/memory/types.ts"
      provides: "Interface MemoryProvider.prefetch com entityFilter"
    - path: "packages/bot/src/handlers/text.ts"
      provides: "Carrega agent e passa ao ContextBuilder"
    - path: "packages/core/src/ws-server.ts"
      provides: "Carrega agent e passa ao ContextBuilder"
  key_links:
    - from: "text.ts"
      to: "context-builder.ts"
      via: "new ContextBuilder(config, harnessLoader).build(text, chatId, topicId, sessionId, agentConfig)"
    - from: "context-builder.ts"
      to: "manager.ts"
      via: "memoryManagerV2.prefetchAll(userMessage, source, sessionId, entityFilter)"
    - from: "manager.ts"
      to: "builtin-provider.ts"
      via: "provider.prefetch(query, { sessionId, entityFilter })"
---

# Fase 22 Plano 03: Core Logic -- ContextBuilder + Memory Filtering + Wiring

**Objetivo:** Fazer o ContextBuilder aceitar um AgentConfig opcional, prepend o system_prompt do agente, filtrar memoria por tags quando memory_mode='filtered', e conectar text.ts e ws-server.ts para carregar o agente do topic e passar ao ContextBuilder.

## Contexto

@packages/core/src/context-builder.ts -- build() atual recebe userMessage, chatId, topicId, sessionId
@packages/core/src/memory/manager.ts -- prefetchAll(query, source, sessionId) atual
@packages/core/src/memory/types.ts -- interface MemoryProvider com prefetch(query, {sessionId})
@packages/core/src/memory/builtin-provider.ts -- prefetch(query, {sessionId}) usa FTS5 + store.search
@packages/core/src/memory/builtin-store.ts -- search() usa stateStore.searchMemoryEntries
@packages/bot/src/handlers/text.ts -- cria ContextBuilder, chama build(), resolve runtime
@packages/core/src/ws-server.ts -- cria ContextBuilder no handleSend, chama build()
@packages/core/src/state-store.ts -- getAgent(), getTopicByChatAndThread() com agent_id

## Tarefas

<task id="1" type="auto">
<files>packages/core/src/memory/types.ts</files>
<action>
Atualizar a interface MemoryProvider para incluir entityFilter no contexto de prefetch e queuePrefetch.

O arquivo `packages/core/src/memory/types.ts` tem a interface MemoryProvider (linha 65-83) com 3 metodos que recebem `ctx: { sessionId?: string }`: prefetch (linha 73), queuePrefetch (linha 74), e syncTurn (linha 76).

Atualizar APENAS prefetch e queuePrefetch (NAO syncTurn):

**1) Linha 73 -- prefetch:**

De:
```typescript
  prefetch(query: string, ctx: { sessionId?: string }): Promise<PrefetchResult | null>;
```

Para:
```typescript
  prefetch(query: string, ctx: { sessionId?: string; entityFilter?: string[] }): Promise<PrefetchResult | null>;
```

**2) Linha 74 -- queuePrefetch:**

De:
```typescript
  queuePrefetch(query: string, ctx: { sessionId?: string }): void;
```

Para:
```typescript
  queuePrefetch(query: string, ctx: { sessionId?: string; entityFilter?: string[] }): void;
```

NAO alterar syncTurn (linha 76) -- nao precisa de entityFilter.
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -c 'entityFilter' packages/core/src/memory/types.ts</automated></verify>
<done>Interface MemoryProvider.prefetch e queuePrefetch aceitam entityFilter opcional. 2 ocorrencias de entityFilter no arquivo.</done>
</task>

<task id="2" type="auto">
<files>packages/core/src/memory/builtin-provider.ts</files>
<action>
Atualizar o metodo `prefetch` do BuiltinMemoryProvider para aceitar `entityFilter` opcional.

**1) Atualizar a assinatura de prefetch (linha ~56):**

De:
```typescript
async prefetch(query: string, ctx: { sessionId?: string } = {}): Promise<PrefetchResult | null> {
```

Para:
```typescript
async prefetch(query: string, ctx: { sessionId?: string; entityFilter?: string[] } = {}): Promise<PrefetchResult | null> {
```

**2) Modificar a busca de memHits para aplicar filtro quando entityFilter esta presente:**

Substituir:
```typescript
// 1) FTS5 over memory_entries
const memHits = this.store.search(query, 5);
```

Por:
```typescript
// 1) FTS5 over memory_entries -- filter by entity tags when agent has filtered memory mode
let memHits = this.store.search(query, ctx.entityFilter ? 20 : 5);

// When entityFilter is active, post-filter hits to only include memories
// that have at least one memory_ref.entity_name matching the filter tags.
// Pre-fetch more hits (20) so filtering doesn't starve the result set.
if (ctx.entityFilter && ctx.entityFilter.length > 0 && memHits.length > 0) {
  const filterSet = new Set(ctx.entityFilter.map(t => t.toLowerCase()));
  memHits = memHits.filter(h => {
    const refs = stateStore.listMemoryRefs(h.entry.id);
    return refs.some(r => filterSet.has(r.entityName.toLowerCase()));
  });
  memHits = memHits.slice(0, 5);
}
```

**3) Adicionar import de stateStore se nao existir:**
O import `import { stateStore } from '../state-store';` ja existe no arquivo (linha 27). Nenhuma mudanca necessaria.
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -c 'entityFilter' packages/core/src/memory/builtin-provider.ts</automated></verify>
<done>prefetch aceita entityFilter opcional. Quando presente, pre-filtra hits via memory_refs.entity_name matching. Busca 20 hits pre-filtro para garantir 5 pos-filtro.</done>
</task>

<task id="3" type="auto">
<files>packages/core/src/memory/manager.ts</files>
<action>
Atualizar `prefetchAll` do MemoryManager para aceitar e propagar `entityFilter`.

**1) Atualizar a assinatura de prefetchAll (linha ~207):**

De:
```typescript
async prefetchAll(query: string, source: 'context_builder' | 'tool_search' = 'context_builder', sessionId?: string): Promise<string> {
```

Para:
```typescript
async prefetchAll(query: string, source: 'context_builder' | 'tool_search' = 'context_builder', sessionId?: string, entityFilter?: string[]): Promise<string> {
```

**2) Atualizar a chamada a provider.prefetch dentro do for-loop (linha ~215):**

De:
```typescript
const res = await p.prefetch(query, { sessionId: sessionId ?? this.initCtx?.sessionId });
```

Para:
```typescript
const res = await p.prefetch(query, { sessionId: sessionId ?? this.initCtx?.sessionId, entityFilter });
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep 'entityFilter' packages/core/src/memory/manager.ts | wc -l</automated></verify>
<done>prefetchAll aceita entityFilter opcional e propaga para cada provider.prefetch. 2 ocorrencias no arquivo.</done>
</task>

<task id="4" type="auto">
<files>packages/core/src/context-builder.ts</files>
<action>
Atualizar ContextBuilder para aceitar AgentConfig opcional e usar seus dados.

**1) Adicionar import de AgentConfig no topo:**
```typescript
import type { ForgeClawConfig, AgentConfig } from './types';
```
(Substituir o import existente `import type { ForgeClawConfig } from './types';` na linha 5)

**2) Atualizar a assinatura do metodo build (linha ~34):**

De:
```typescript
async build(userMessage: string, _chatId: number, topicId: number, sessionId?: string | null): Promise<string> {
```

Para:
```typescript
async build(userMessage: string, _chatId: number, topicId: number, sessionId?: string | null, agent?: AgentConfig | null): Promise<string> {
```

**3) Apos o `const parts: string[] = [];` (linha ~35), adicionar injecao do system_prompt do agente:**

```typescript
// 0. Agent system prompt -- prepended BEFORE everything else so it acts
//    as a persona/instruction layer that wraps the entire context.
if (agent?.systemPrompt) {
  parts.push(`# Agente: ${agent.name}\n\n${agent.systemPrompt}`);
}
```

**4) Atualizar a chamada a memoryManagerV2.prefetchAll para passar entityFilter do agente:**

De:
```typescript
const memBlock = await memoryManagerV2.prefetchAll(userMessage, 'context_builder', sessionId ?? undefined);
```

Para:
```typescript
const entityFilter = agent?.memoryMode === 'filtered' && agent.memoryDomainFilter.length > 0
  ? agent.memoryDomainFilter
  : undefined;
const memBlock = await memoryManagerV2.prefetchAll(userMessage, 'context_builder', sessionId ?? undefined, entityFilter);
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -c 'AgentConfig' packages/core/src/context-builder.ts && grep -c 'entityFilter' packages/core/src/context-builder.ts && grep -c 'systemPrompt' packages/core/src/context-builder.ts</automated></verify>
<done>ContextBuilder.build() aceita AgentConfig opcional. Prependa system_prompt do agente. Passa entityFilter ao prefetchAll quando agent tem memory_mode='filtered'.</done>
</task>

<task id="5" type="auto">
<files>packages/bot/src/handlers/text.ts</files>
<action>
Atualizar text.ts para carregar o agente do topic e passar ao ContextBuilder.

**1) Importar AgentConfig (ja importa types de @forgeclaw/core):**

Adicionar `type AgentConfig` ao import existente de `@forgeclaw/core` na linha 16:
```typescript
import type { StreamEvent, ForgeClawConfig, RuntimeName, AgentConfig } from '@forgeclaw/core';
```

**2) No processMessage(), APOS o bloco que resolve o topicRow (linha ~146, onde tem `const topicRow = stateStore.getTopicByChatAndThread(chatId, topicId);`), carregar o agente:**

Adicionar imediatamente APOS `const topicRow = ...`:

```typescript
// Load agent linked to this topic (if any)
let agentConfig: AgentConfig | null = null;
if (topicRow?.agentId) {
  agentConfig = stateStore.getAgent(topicRow.agentId);
  if (agentConfig) {
    console.log(`[text-handler] topic ${sessionKey} linked to agent '${agentConfig.name}'`);
  }
}
```

**3) Atualizar a chamada ao ContextBuilder.build() para passar agentConfig:**

De (linha ~140):
```typescript
const enrichedPrompt = await contextBuilder.build(text, chatId, topicId ?? 0, session.claudeSessionId);
```

Para:
```typescript
const enrichedPrompt = await contextBuilder.build(text, chatId, topicId ?? 0, session.claudeSessionId, agentConfig);
```

**4) Atualizar resolucao de runtime para considerar o agente:**

Mudar de (linha ~148):
```typescript
const requestedRuntime = (topicRow?.runtime ?? config.defaultRuntime) as RuntimeName | undefined;
```

Para:
```typescript
// Agent runtime > topic runtime > config default
const requestedRuntime = (agentConfig?.defaultRuntime ?? topicRow?.runtime ?? config.defaultRuntime) as RuntimeName | undefined;
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -c 'agentConfig' packages/bot/src/handlers/text.ts && grep -c 'getAgent' packages/bot/src/handlers/text.ts</automated></verify>
<done>text.ts carrega agente via topicRow.agentId, passa ao ContextBuilder.build(), e usa agent.defaultRuntime na resolucao de runtime (prioridade: agent > topic > config).</done>
</task>

<task id="6" type="auto">
<files>packages/core/src/ws-server.ts</files>
<action>
Atualizar ws-server.ts (handleSend) para carregar o agente do topic e passar ao ContextBuilder. Mesmo pattern do text.ts.

**1) Adicionar import de AgentConfig na linha 15:**
```typescript
import type { StreamEvent, SessionInfo, RuntimeName, AgentConfig } from './types';
```
(Substituir o import existente `import type { StreamEvent, SessionInfo, RuntimeName } from './types';`)

**2) No handleSend(), APOS o bloco que resolve topicRow (linha ~185-188), carregar o agente:**

Apos `const topicRow = ...` e ANTES de `const cfgForRuntime = ...`, adicionar:

```typescript
// Load agent linked to this topic (if any)
let agentConfig: AgentConfig | null = null;
if (topicRow?.agentId) {
  agentConfig = stateStore.getAgent(topicRow.agentId);
  if (agentConfig) {
    console.log(`[ws-server] topic ${sessionKey} linked to agent '${agentConfig.name}'`);
  }
}
```

**3) Atualizar resolucao de runtime para considerar o agente (linha ~192):**

De:
```typescript
const requestedRuntime = (topicRow?.runtime ?? cfgForRuntime.defaultRuntime) as RuntimeName | undefined;
```

Para:
```typescript
// Agent runtime > topic runtime > config default
const requestedRuntime = (agentConfig?.defaultRuntime ?? topicRow?.runtime ?? cfgForRuntime.defaultRuntime) as RuntimeName | undefined;
```

**4) Atualizar a chamada ao ContextBuilder.build() para passar agentConfig (linha ~204):**

De:
```typescript
enrichedPrompt = await builder.build(message, chatId, topicId ?? 0);
```

Para:
```typescript
enrichedPrompt = await builder.build(message, chatId, topicId ?? 0, undefined, agentConfig);
```

(Note: o ws-server nao passa sessionId ao build -- manter undefined para backward compat.)
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -c 'agentConfig' packages/core/src/ws-server.ts && grep -c 'getAgent' packages/core/src/ws-server.ts</automated></verify>
<done>ws-server.ts carrega agente via topicRow.agentId, passa ao ContextBuilder.build(), e usa agent.defaultRuntime na resolucao de runtime.</done>
</task>

## Criterios de Sucesso

- [ ] MemoryProvider interface tem entityFilter em prefetch e queuePrefetch
- [ ] ContextBuilder.build() aceita AgentConfig opcional como 5o parametro
- [ ] system_prompt do agente e prepended ao contexto quando presente
- [ ] memory_mode='filtered' causa filtragem via entityFilter no prefetchAll
- [ ] memory_mode='global' mantem comportamento atual (sem filtro)
- [ ] BuiltinMemoryProvider.prefetch filtra por entity_name via memory_refs quando entityFilter presente
- [ ] text.ts carrega agent do topic e passa ao ContextBuilder + usa agent.defaultRuntime
- [ ] ws-server.ts carrega agent do topic e passa ao ContextBuilder + usa agent.defaultRuntime
- [ ] Cadeia completa: topic -> agent -> systemPrompt prepended + memoria filtrada
