---
phase: 18-core-hardening
plan: 18-02
type: fix
autonomous: true
wave: 1
depends_on: [18-01]
requirements: [MED-M2, MED-M10]
must_haves:
  truths:
    - "v1 memoryManager compile cron does NOT run at bot startup"
    - "v2 memoryManagerV2 crons (writer + janitor) still run normally"
    - "v1 memoryManager object is still alive for addEntry/getDailyLog calls"
    - "Claude CLI errors show friendly messages instead of raw error text"
    - "Rate limit, auth, and not-found errors are detected and translated"
  artifacts:
    - path: "packages/bot/src/index.ts"
      provides: "v1 compile cron removed from startup"
    - path: "packages/bot/src/handlers/text.ts"
      provides: "Friendly error messages for common Claude CLI failures"
  key_links:
    - from: "bot/src/index.ts"
      to: "@forgeclaw/core memoryManager"
      via: "singleton import — keep object, remove startCompileCron()"
    - from: "text.ts catch block"
      to: "claude-runner.ts errors"
      via: "pattern matching on error message strings"
---

# Fase 18 Plano 02: Deprecate v1 Memory Cron + Friendly Error Messages

**Objetivo:** (M2) Remover o cron de compile-daily do v1 MemoryManager para eliminar overlap com v2 janitor/writer. (M10) Traduzir erros crus do Claude CLI em mensagens amigaveis para o Telegram.

## Contexto

@packages/bot/src/index.ts -- linhas 216-217: `memoryManager.startCompileCron()` e shutdown `memoryManager.stopCompileCron()`
@packages/bot/src/handlers/text.ts -- catch block linhas 316-320: envia `Error: ${errorMsg}` raw
@packages/core/src/memory-manager.ts -- v1 com compileDaily + startCompileCron + stopCompileCron
@packages/core/src/memory/manager.ts -- v2 com startCrons() (writer hourly + janitor 02:55 UTC)

## Tarefas

<task id="1" type="auto">
<files>packages/bot/src/index.ts</files>
<action>
Remover a linha que inicia o cron v1 e o stop no shutdown. O v1 memoryManager singleton CONTINUA importado e ativo (usado por addEntry em event listeners).

1. **Remover linha 216:** `memoryManager.startCompileCron();`
   E o comentario na linha 215: `// Schedule the internal compile-daily job (legacy simple compile).`

2. **No shutdown handler** (linha 343), remover: `memoryManager.stopCompileCron();`

3. Adicionar um comentario onde o startCompileCron estava, explicando a deprecacao:

```typescript
// v1 MemoryManager compile cron DEPRECATED (M2): the v2 memory system
// (writer + janitor crons in memoryManagerV2.startCrons()) handles daily
// log extraction and distillation. v1 memoryManager singleton is kept
// alive for backward-compat addEntry()/getDailyLog() calls from event
// listeners below, but its scheduled compile task is removed to prevent
// duplicate processing and potential race conditions with v2.
```

4. No shutdown, simplificar removendo o stopCompileCron:
```typescript
// ANTES:
// memoryManager.stopCompileCron();
// try { await memoryManagerV2.shutdownAll(); } catch {}

// DEPOIS:
try { await memoryManagerV2.shutdownAll(); } catch {}
```

NAO remover o import de `memoryManager` (ele e usado nas linhas 282, 303 para `addEntry`).
NAO remover as chamadas a `memoryManager.addEntry()` nos event listeners (linhas 278-286, 300-306) -- esses CONTINUAM funcionando, escrevem no daily log que o v2 writer/janitor depois processam.
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -n 'startCompileCron\|stopCompileCron' packages/bot/src/index.ts; echo "GREP_EXIT: $?"</automated></verify>
<done>Nenhuma chamada a startCompileCron ou stopCompileCron existe no bot/index.ts. memoryManager continua importado e usado para addEntry.</done>
</task>

<task id="2" type="auto">
<files>packages/core/src/memory-manager.ts</files>
<action>
Marcar os metodos de cron como deprecated (NAO remover -- podem ter chamadores externos):

```typescript
/**
 * @deprecated v1 compile cron replaced by v2 writer+janitor system.
 * Kept for backward compatibility. Do not call in new code.
 */
startCompileCron(schedule: string = '55 23 * * *'): void {
```

```typescript
/**
 * @deprecated v1 compile cron replaced by v2 writer+janitor system.
 */
stopCompileCron(): void {
```

```typescript
/**
 * @deprecated v1 compile replaced by v2 janitor distillation.
 * Kept for backward compatibility.
 */
async compileDaily(): Promise<void> {
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -c '@deprecated' packages/core/src/memory-manager.ts</automated></verify>
<done>3 metodos marcados como @deprecated: startCompileCron, stopCompileCron, compileDaily.</done>
</task>

<task id="3" type="auto">
<files>packages/bot/src/handlers/text.ts</files>
<action>
No catch block da funcao processMessage (linhas 316-320), substituir o envio de erro raw por deteccao e traducao de erros comuns.

**Localizar o catch block atual** (dentro do try que envolve o `for await (const event of runner.run(...))`):

```typescript
} catch (err) {
  const errorMsg = err instanceof Error ? err.message : String(err);
  console.error(`[text-handler] Error processing message:`, err);
  await safeEditText(
    ctx,
    chatId,
    placeholder.message_id,
    `Error: ${errorMsg}`,
  );
}
```

**Substituir por:**

```typescript
} catch (err) {
  const errorMsg = err instanceof Error ? err.message : String(err);
  console.error(`[text-handler] Error processing message:`, err);

  // M10: Translate common Claude CLI errors to friendly messages
  // instead of forwarding raw error text to Telegram.
  const friendlyMsg = classifyClaudeError(errorMsg);
  await safeEditText(
    ctx,
    chatId,
    placeholder.message_id,
    friendlyMsg,
  );
}
```

**Adicionar a funcao `classifyClaudeError` ANTES da funcao `safeEditText`** (ou no final do arquivo, antes do ultimo export):

```typescript
/**
 * Detect common Claude CLI error patterns and return a user-friendly
 * message in Portuguese. Falls back to a generic message for unknown errors.
 *
 * Known patterns:
 * - Auth expired/invalid: "not authenticated", "auth", "unauthorized", "401"
 * - Rate limited: "rate limit", "429", "too many requests", "overloaded"
 * - CLI not found: "ENOENT", "not found", "command not found"
 * - Context window: "context window", "context_length", "too long"
 * - Session expired: "No conversation found", "session"
 * - Network: "ECONNREFUSED", "ETIMEDOUT", "fetch failed"
 * - Generic CLI failure: "exited with code"
 */
function classifyClaudeError(errorMsg: string): string {
  const lower = errorMsg.toLowerCase();

  // Auth issues
  if (
    lower.includes('not authenticated') ||
    lower.includes('unauthorized') ||
    lower.includes('auth') ||
    lower.includes('401') ||
    lower.includes('invalid api key') ||
    lower.includes('invalid_api_key')
  ) {
    return 'Claude CLI nao esta autenticado. Rode `claude auth login` no servidor para reconectar.';
  }

  // Rate limiting
  if (
    lower.includes('rate limit') ||
    lower.includes('rate_limit') ||
    lower.includes('429') ||
    lower.includes('too many requests') ||
    lower.includes('overloaded')
  ) {
    return 'Rate limit atingido na API. Aguarde alguns minutos e tente novamente.';
  }

  // CLI not found / not installed
  if (
    lower.includes('enoent') ||
    (lower.includes('not found') && (lower.includes('claude') || lower.includes('command'))) ||
    lower.includes('command not found')
  ) {
    return 'Claude CLI nao encontrado no servidor. Verifique a instalacao com `which claude`.';
  }

  // Context window exceeded
  if (
    lower.includes('context window') ||
    lower.includes('context_length') ||
    lower.includes('too long') ||
    lower.includes('maximum context')
  ) {
    return 'Contexto excedeu o limite. Use /new para iniciar uma sessao limpa.';
  }

  // Session expired (already handled by runner retry, but just in case)
  if (
    lower.includes('no conversation found') ||
    lower.includes('session not found')
  ) {
    return 'Sessao expirou. Use /new para iniciar uma nova.';
  }

  // Network errors
  if (
    lower.includes('econnrefused') ||
    lower.includes('etimedout') ||
    lower.includes('fetch failed') ||
    lower.includes('network')
  ) {
    return 'Erro de rede ao conectar com a API. Verifique a conexao do servidor.';
  }

  // Generic CLI exit code (keep the code for debugging)
  const exitMatch = errorMsg.match(/exited with code (\d+)/);
  if (exitMatch) {
    return `Claude CLI falhou (exit code ${exitMatch[1]}). Verifique os logs com \`forgeclaw logs\`.`;
  }

  // Unknown error — show truncated message (max 200 chars) to avoid
  // leaking internal details while still being debuggable.
  const truncated = errorMsg.length > 200 ? errorMsg.slice(0, 200) + '...' : errorMsg;
  return `Erro ao processar: ${truncated}`;
}
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && npx tsc --noEmit --project packages/bot/tsconfig.json 2>&1 | head -20</automated></verify>
<done>catch block usa classifyClaudeError() que retorna mensagens amigaveis em portugues para auth, rate limit, not found, context, session, network. Erros desconhecidos sao truncados em 200 chars.</done>
</task>

<task id="4" type="auto">
<files>packages/bot/src/handlers/text.ts</files>
<action>
Verificacao de integracao: garantir que o outro catch block no handler (o `handleInterrupt` e o outer finally) NAO expoe erros raw.

Verificar:
1. A funcao `handleInterrupt` (linhas 343-369) -- nao tem catch com erro visivel, OK.
2. O finally block do processMessage (linhas 328-340) -- nao envia mensagem, OK.
3. A funcao `safeEditText` (linhas 375-409) -- ja trata erros de parse HTML, OK.

Confirmar que o `typingInterval` declarado na linha 165 e limpo em ambos os paths (success e error). Atualmente o `clearInterval(typingInterval)` esta no finally interno (linha 326). Verificar que a variavel esta acessivel no catch.

ATENCAO: Olhando o codigo, `typingInterval` e declarado DENTRO do try (linha 165: `const typingInterval = setInterval(...)`). O `clearInterval` esta no finally desse mesmo try (linha 326). No catch (linhas 316-320), NAO ha clearInterval. Isso significa que se o runner.run() falha no PRIMEIRO yield, o typing interval pode ter sido criado mas o finally do inner try cuidara dele.

Na verdade, revendo o codigo: o `clearInterval(typingInterval)` (linha 326) esta DENTRO do finally do bloco try que contem o `for await`. Ele esta no escopo certo e sera chamado em caso de erro. NAO ha problema.

Rodar `npx tsc --noEmit` para confirmar tipo correto.
</action>
<verify><automated>cd /home/projects/ForgeClaw && npx tsc --noEmit --project packages/bot/tsconfig.json 2>&1 | head -20</automated></verify>
<done>Nenhum path de erro expoe mensagens raw ao Telegram. clearInterval e chamado corretamente em todos os paths.</done>
</task>

<task id="5" type="auto">
<files>
packages/bot/src/index.ts
packages/bot/src/handlers/text.ts
packages/core/src/memory-manager.ts
</files>
<action>
Verificacao final de integracao:

1. Confirmar que `memoryManager.addEntry()` ainda funciona (chamado em event listeners no index.ts linhas 282, 303):
   - Estas chamadas NAO dependem do cron -- elas apenas escrevem no daily log file
   - O v2 writer cron le esses mesmos daily logs
   - Pipeline: addEntry() -> daily log file -> v2 writer (hourly) -> v2 janitor (02:55 UTC)

2. Confirmar que `memoryManager` singleton nao tem side effects ao nao chamar startCompileCron:
   - Constructor nao inicia cron automaticamente (cron so inicia via startCompileCron)
   - Sem startCompileCron, o objeto funciona normalmente para reads/writes

3. Confirmar pipeline end-to-end:
   - User sends message -> text handler -> event `message:incoming` -> memoryManager.addEntry()
   - v2 writer cron (hourly) -> processes daily logs -> extracts bullets
   - v2 janitor cron (02:55 UTC) -> compresses dailies, distills to memory_entries
   - No overlap com v1 compileDaily (removido)

4. Build final:
```bash
cd /home/projects/ForgeClaw && npx tsc --noEmit --project packages/core/tsconfig.json && npx tsc --noEmit --project packages/bot/tsconfig.json && echo "BUILD OK"
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && npx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | tail -5 && npx tsc --noEmit --project packages/bot/tsconfig.json 2>&1 | tail -5</automated></verify>
<done>Build passa em core e bot. v1 cron removido, v2 crons intactos, addEntry() funciona, pipeline de memoria e linear sem overlap.</done>
</task>

## Criterios de Sucesso

- [ ] `startCompileCron` e `stopCompileCron` NAO sao chamados no bot/index.ts
- [ ] memoryManager singleton continua importado e usado para addEntry
- [ ] memoryManagerV2.startCrons() continua sendo chamado normalmente
- [ ] Erro de auth mostra "Claude CLI nao esta autenticado..."
- [ ] Erro de rate limit mostra "Rate limit atingido..."
- [ ] Erro desconhecido mostra mensagem truncada (max 200 chars)
- [ ] TypeScript compila sem erros em core e bot
