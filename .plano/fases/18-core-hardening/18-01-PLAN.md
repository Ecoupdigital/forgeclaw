---
phase: 18-core-hardening
plan: 18-01
type: fix
autonomous: true
wave: 0
depends_on: []
requirements: [MED-M1, MED-M3, MED-M8]
must_haves:
  truths:
    - "Daily log dir resolves to ~/.forgeclaw/memory/daily when no vaultPath configured"
    - "Daily log dir resolves to {vaultPath}/05-pessoal/daily-log when vaultPath configured"
    - "FORGECLAW_DAILY_LOG_DIR env var overrides both defaults"
    - "skipPermissions config field controls --dangerously-skip-permissions flag"
    - "Installer warns when bun version < 1.1.0"
  artifacts:
    - path: "packages/core/src/memory-manager.ts"
      provides: "Config-aware daily dir resolution"
    - path: "packages/core/src/context-builder.ts"
      provides: "Config-aware daily dir in stat line"
    - path: "packages/core/src/memory/janitor.ts"
      provides: "Config-aware daily dir in janitor"
    - path: "packages/core/src/memory/writer.ts"
      provides: "Config-aware daily dir in writer"
    - path: "packages/dashboard/src/lib/core.ts"
      provides: "Config-aware daily dir in dashboard"
    - path: "packages/core/src/claude-runner.ts"
      provides: "Configurable skipPermissions"
    - path: "packages/core/src/types.ts"
      provides: "skipPermissions field on ForgeClawConfig"
    - path: "packages/core/src/config.ts"
      provides: "skipPermissions parsed from config"
    - path: "packages/cli/src/commands/install.ts"
      provides: "Bun version check with minimum 1.1.0"
  key_links:
    - from: "memory-manager.ts"
      to: "config.ts"
      via: "getConfig() for vaultPath"
    - from: "claude-runner.ts"
      to: "config.ts"
      via: "getConfig() for skipPermissions"
---

# Fase 18 Plano 01: Config-Aware Daily Dir + skipPermissions + Bun Version Check

**Objetivo:** Eliminar 3 gaps MEDIUM: (M1) remover hardcoded `/home/vault/05-pessoal/daily-log` de 5 arquivos, usando config.vaultPath com fallback seguro; (M3) tornar `--dangerously-skip-permissions` configuravel via config; (M8) checar versao minima do bun no installer.

## Contexto

@packages/core/src/memory-manager.ts -- v1 MemoryManager, constructor recebe dailyDir com hardcoded fallback
@packages/core/src/context-builder.ts -- buildStatLine() hardcodes daily dir
@packages/core/src/memory/janitor.ts -- DEFAULT_DAILY_DIR hardcoded no topo
@packages/core/src/memory/writer.ts -- DEFAULT_DAILY_DIR hardcoded no topo
@packages/dashboard/src/lib/core.ts -- DAILY_DIR hardcoded no topo
@packages/core/src/claude-runner.ts -- buildArgs() sempre adiciona --dangerously-skip-permissions
@packages/core/src/types.ts -- ForgeClawConfig interface
@packages/core/src/config.ts -- validateConfig function
@packages/cli/src/commands/install.ts -- checkDependency() so faz `which bun`

## Tarefas

<task id="1" type="auto">
<files>packages/core/src/types.ts</files>
<action>
Adicionar campo `skipPermissions` a interface `ForgeClawConfig`:

```typescript
/**
 * Whether to pass --dangerously-skip-permissions to the Claude CLI.
 * Default: true (current behavior). Set to false to run in interactive
 * permission mode — useful for auditing what tools Claude uses.
 *
 * SECURITY NOTE: When true, Claude Code runs with full filesystem and
 * shell access without prompting. This is required for unattended
 * Telegram bot operation but means the LLM can execute arbitrary
 * commands in the bot's working directory.
 */
skipPermissions?: boolean;
```

Inserir APOS o campo `timezone?: string;` (linha 44) e ANTES do fechamento `}` da interface.
</action>
<verify><automated>cd /home/projects/ForgeClaw && npx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | head -20</automated></verify>
<done>ForgeClawConfig tem campo skipPermissions?: boolean com JSDoc explicando o risco de seguranca.</done>
</task>

<task id="2" type="auto">
<files>packages/core/src/config.ts</files>
<action>
Na funcao `validateConfig`, adicionar parsing do campo `skipPermissions` no objeto de retorno.

Encontrar a linha `timezone: typeof obj.timezone === 'string' ? obj.timezone : 'America/Sao_Paulo',` (linha 57) e adicionar ABAIXO (antes do `};`):

```typescript
skipPermissions: typeof obj.skipPermissions === 'boolean' ? obj.skipPermissions : true,
```

Default e `true` para manter backward compatibility (comportamento atual).
</action>
<verify><automated>cd /home/projects/ForgeClaw && npx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | head -20</automated></verify>
<done>validateConfig parseia skipPermissions como boolean com default true.</done>
</task>

<task id="3" type="auto">
<files>packages/core/src/claude-runner.ts</files>
<action>
Modificar `buildArgs()` para ler config e condicionalmente adicionar `--dangerously-skip-permissions`.

1. Adicionar import no topo do arquivo:
```typescript
import { getConfig } from './config';
```

2. Mudar a assinatura de `buildArgs` de sincrona para:
```typescript
private async buildArgs(options: ClaudeRunnerOptions): Promise<string[]>
```

ATENCAO: isso requer mudar `spawnAndStream` tambem, pois chama `this.buildArgs(options)`. A linha `const args = this.buildArgs(options);` deve virar `const args = await this.buildArgs(options);` (spawnAndStream ja e async, entao nao quebra nada).

3. Na funcao `buildArgs`, substituir a linha 224:
```typescript
args.push('--dangerously-skip-permissions');
```

Por:
```typescript
// SECURITY: --dangerously-skip-permissions allows Claude Code to execute
// any tool (Read, Write, Bash, etc.) without interactive approval. Required
// for unattended bot operation. Disable via config skipPermissions: false
// to audit tool usage in development/testing.
try {
  const config = await getConfig();
  if (config.skipPermissions !== false) {
    args.push('--dangerously-skip-permissions');
  }
} catch {
  // Config unavailable (e.g. during tests) — default to skip permissions
  // to preserve existing behavior.
  args.push('--dangerously-skip-permissions');
}
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && npx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | head -20</automated></verify>
<done>--dangerously-skip-permissions so e adicionado quando config.skipPermissions !== false. Default true preserva backward compat. Comentario explica risco.</done>
</task>

<task id="4" type="auto">
<files>
packages/core/src/memory-manager.ts
packages/core/src/context-builder.ts
packages/core/src/memory/janitor.ts
packages/core/src/memory/writer.ts
packages/dashboard/src/lib/core.ts
</files>
<action>
Criar funcao helper `resolveDailyDir` e aplicar em todos os 5 arquivos. A logica e identica em todos:

```
1. Se env var FORGECLAW_DAILY_LOG_DIR existe -> usa ela
2. Se config.vaultPath existe -> usa join(config.vaultPath, '05-pessoal', 'daily-log')
3. Senao -> usa join(homedir(), '.forgeclaw', 'memory', 'daily')
```

**Arquivo 1: packages/core/src/memory-manager.ts**

O constructor atual recebe `dailyDir` como parametro com default hardcoded. Mudar o default:

```typescript
constructor(
  memoryDir = join(homedir(), '.forgeclaw', 'memory'),
  harnessDir = join(homedir(), '.forgeclaw', 'harness'),
  dailyDir?: string,
) {
  this.memoryDir = memoryDir;
  // Daily log resolution: env var > config vaultPath > ~/.forgeclaw/memory/daily
  // Note: config is loaded async, so we resolve synchronously with env/default
  // and the bot can override via the dailyDir param after loading config.
  this.dailyDir = dailyDir
    ?? process.env.FORGECLAW_DAILY_LOG_DIR
    ?? join(homedir(), '.forgeclaw', 'memory', 'daily');
  this.harnessDir = harnessDir;
  this.memoryFilePath = join(harnessDir, 'MEMORY.md');
}
```

Na linha 217 onde o singleton e criado (`export const memoryManager = new MemoryManager();`), NAO mudar -- o singleton usa defaults. O bot em `index.ts` pode chamar com vaultPath apos carregar config (tarefa 5 cuida disso).

IMPORTANTE: A razao de nao colocar `getConfig()` aqui e que o constructor e sincrono e `getConfig()` e async. O bot passara o dailyDir correto apos carregar config.

**Arquivo 2: packages/core/src/context-builder.ts**

Na funcao `buildStatLine()`, linhas 72-73:
```typescript
// ANTES:
const dailyDir =
  process.env.FORGECLAW_DAILY_LOG_DIR ?? '/home/vault/05-pessoal/daily-log';

// DEPOIS:
const dailyDir = process.env.FORGECLAW_DAILY_LOG_DIR
  ?? (this.config.vaultPath
    ? join(this.config.vaultPath, '05-pessoal', 'daily-log')
    : join(homedir(), '.forgeclaw', 'memory', 'daily'));
```

Adicionar imports no topo se nao existirem:
- `import { join } from 'node:path';` -- ja existe como `import path from 'node:path'`, usar `path.join` ao inves de `join`
- `import { homedir } from 'node:os';` -- NAO existe, ADICIONAR

Portanto o codigo fica:
```typescript
const dailyDir = process.env.FORGECLAW_DAILY_LOG_DIR
  ?? (this.config.vaultPath
    ? path.join(this.config.vaultPath, '05-pessoal', 'daily-log')
    : path.join(homedir(), '.forgeclaw', 'memory', 'daily'));
```

**Arquivo 3: packages/core/src/memory/janitor.ts**

Linha 28-29, substituir:
```typescript
// ANTES:
const DEFAULT_DAILY_DIR =
  process.env.FORGECLAW_DAILY_LOG_DIR ?? '/home/vault/05-pessoal/daily-log';

// DEPOIS:
import { homedir } from 'node:os';

async function resolveDailyDir(): Promise<string> {
  if (process.env.FORGECLAW_DAILY_LOG_DIR) return process.env.FORGECLAW_DAILY_LOG_DIR;
  try {
    const config = await getConfig();
    if (config.vaultPath) return join(config.vaultPath, '05-pessoal', 'daily-log');
  } catch {
    // Config unavailable — use safe default
  }
  return join(homedir(), '.forgeclaw', 'memory', 'daily');
}
```

Nota: `getConfig` ja e importado na linha 23 (`import { getConfig } from '../config';`). `join` ja importado na linha 19. `homedir` NAO importado -- adicionar.

Na funcao `runJanitor` (linha 488), mudar:
```typescript
// ANTES:
const dailyDir = opts.dailyDir ?? DEFAULT_DAILY_DIR;

// DEPOIS:
const dailyDir = opts.dailyDir ?? await resolveDailyDir();
```

Remover a const `DEFAULT_DAILY_DIR` das linhas 28-29.

**Arquivo 4: packages/core/src/memory/writer.ts**

Linha 21-22, substituir:
```typescript
// ANTES:
const DEFAULT_DAILY_DIR =
  process.env.FORGECLAW_DAILY_LOG_DIR ?? '/home/vault/05-pessoal/daily-log';

// DEPOIS:
import { homedir } from 'node:os';
import { getConfig } from '../config';

async function resolveDailyDir(): Promise<string> {
  if (process.env.FORGECLAW_DAILY_LOG_DIR) return process.env.FORGECLAW_DAILY_LOG_DIR;
  try {
    const config = await getConfig();
    if (config.vaultPath) return join(config.vaultPath, '05-pessoal', 'daily-log');
  } catch {
    // Config unavailable — use safe default
  }
  return join(homedir(), '.forgeclaw', 'memory', 'daily');
}
```

Nota: `join` ja importado na linha 13. `homedir` e `getConfig` NAO importados -- adicionar.

Na funcao `runWriter` (linha 166), mudar:
```typescript
// ANTES:
const dailyDir = opts.dailyDir ?? DEFAULT_DAILY_DIR;

// DEPOIS:
const dailyDir = opts.dailyDir ?? await resolveDailyDir();
```

Remover a const `DEFAULT_DAILY_DIR` das linhas 21-22.

**Arquivo 5: packages/dashboard/src/lib/core.ts**

Linhas 36-37, substituir:
```typescript
// ANTES:
const DAILY_DIR =
  process.env.FORGECLAW_DAILY_LOG_DIR ?? "/home/vault/05-pessoal/daily-log";

// DEPOIS: Resolve at call time, not module load time (config may not be loaded yet)
async function resolveDailyDir(): Promise<string> {
  if (process.env.FORGECLAW_DAILY_LOG_DIR) return process.env.FORGECLAW_DAILY_LOG_DIR;
  try {
    const config = await getConfig();
    if (config && config.vaultPath) return join(config.vaultPath as string, "05-pessoal", "daily-log");
  } catch {
    // fall through
  }
  return join(homedir(), ".forgeclaw", "memory", "daily");
}
```

ATENCAO: O `getConfig()` no dashboard (em core.ts) ja existe e le o config.json diretamente. A funcao ja e importada no escopo do modulo (veja linha 893). O `vaultPath` vem do JSON parsed.

Entao atualizar as funcoes que usam `DAILY_DIR`:

Em `readDailyLog` (linha 852):
```typescript
export async function readDailyLog(date: string): Promise<string | null> {
  try {
    if (!/^\d{4}-\d{2}-\d{2}$/.test(date)) return null;
    const dailyDir = await resolveDailyDir();
    const filePath = join(dailyDir, `${date}.md`);
    if (!existsSync(filePath)) return null;
    return await readFile(filePath, "utf-8");
  } catch {
    return null;
  }
}
```

Em `listDailyLogs` (linha 863):
```typescript
export async function listDailyLogs(): Promise<DailyLog[] | null> {
  try {
    const dailyDir = await resolveDailyDir();
    if (!existsSync(dailyDir)) return null;
    // ... rest stays the same, just replace DAILY_DIR with dailyDir
```

Remover a const `DAILY_DIR` das linhas 36-37.

NOTA sobre o tipo: O `getConfig()` do dashboard retorna `ForgeClawConfig | null`. O tipo `ForgeClawConfig` local (em `packages/dashboard/src/lib/types.ts`) pode nao ter `vaultPath`. Verificar e, se necessario, acessar como `(config as any).vaultPath` ou adicionar o campo ao tipo local. O mais seguro e usar `(config as Record<string, unknown>)?.vaultPath` com check `typeof ... === 'string'`.
</action>
<verify><automated>cd /home/projects/ForgeClaw && npx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | head -30 && npx tsc --noEmit --project packages/dashboard/tsconfig.json 2>&1 | head -30</automated></verify>
<done>Nenhum arquivo contem '/home/vault/05-pessoal/daily-log' hardcoded. Todos resolvem via env > config.vaultPath > ~/.forgeclaw/memory/daily.</done>
</task>

<task id="5" type="auto">
<files>packages/bot/src/index.ts</files>
<action>
Apos o config ser carregado (linha 30: `const config = await getConfig();`), resolver o dailyDir e passar para o MemoryManager v1:

Adicionar APOS linha 30, ANTES de qualquer uso do memoryManager:

```typescript
// M1: Configure v1 memory manager daily dir from config
// The singleton was created with default path; update it now that config is loaded.
import { join } from 'node:path';
import { homedir } from 'node:os';
```

ATENCAO: `join` e `homedir` ja sao importados (linha 3-4). `memoryManager` ja e importado da core (linha 14). O problema e que `memoryManager` e um singleton criado com defaults. Precisamos expor um setter.

**Abordagem alternativa (mais simples):** Em `packages/core/src/memory-manager.ts`, adicionar um metodo publico para atualizar o dailyDir:

```typescript
/** Update daily dir after config is loaded (bot startup). */
setDailyDir(dir: string): void {
  this.dailyDir = dir;
}
```

Entao em `packages/bot/src/index.ts`, apos `const config = await getConfig();` (linha 30), adicionar:

```typescript
// M1: Resolve daily log dir from config — the singleton was created with
// the safe default (~/.forgeclaw/memory/daily). Now that config is loaded,
// update to vault path if configured.
{
  const resolvedDailyDir = process.env.FORGECLAW_DAILY_LOG_DIR
    ?? (config.vaultPath
      ? join(config.vaultPath, '05-pessoal', 'daily-log')
      : join(homedir(), '.forgeclaw', 'memory', 'daily'));
  memoryManager.setDailyDir(resolvedDailyDir);
  console.log(`[forgeclaw] daily log dir: ${resolvedDailyDir}`);
}
```

Isso garante que TODOS os usos do singleton memoryManager (addEntry, getDailyLog, etc.) usam o path correto.

`join` ja esta importado via `import { join } from 'node:path'` (verificar -- se nao, adicionar).
`homedir` ja importado via `import { homedir } from 'node:os'` (verificar no topo -- existe na linha 3).
</action>
<verify><automated>cd /home/projects/ForgeClaw && npx tsc --noEmit --project packages/bot/tsconfig.json 2>&1 | head -20</automated></verify>
<done>Bot startup configura dailyDir do memoryManager v1 singleton baseado em config.vaultPath. Log mostra path resolvido.</done>
</task>

<task id="6" type="auto">
<files>packages/cli/src/commands/install.ts</files>
<action>
Apos o check `hasBun` (linhas 81-90), adicionar verificacao de versao.

Substituir o bloco `if (hasBun) { ... }` (linhas 86-90) por:

```typescript
if (hasBun) {
  // M8: Check minimum bun version (1.1.0)
  let bunVersion = 'unknown';
  try {
    const versionProc = Bun.spawn(['bun', '--version'], { stdout: 'pipe', stderr: 'pipe' });
    const versionOutput = await new Response(versionProc.stdout).text();
    await versionProc.exited;
    bunVersion = versionOutput.trim(); // e.g. "1.2.3"
  } catch {
    // version check failed, continue anyway
  }

  const MIN_BUN_VERSION = '1.1.0';
  if (bunVersion !== 'unknown' && compareSemver(bunVersion, MIN_BUN_VERSION) < 0) {
    log.warn(`bun: version ${bunVersion} detected, minimum recommended is ${MIN_BUN_VERSION}`);
    log.warn('Some features may not work correctly. Update with: bun upgrade');
  } else {
    log.success(`bun: installed (${bunVersion})`);
  }
} else {
  log.error('bun: not found. Install from https://bun.sh')
  process.exit(1)
}
```

Adicionar a funcao helper `compareSemver` ACIMA da funcao `install`:

```typescript
/**
 * Compare two semver strings. Returns negative if a < b, 0 if equal, positive if a > b.
 * Only handles major.minor.patch (no pre-release tags).
 */
function compareSemver(a: string, b: string): number {
  const pa = a.split('.').map(Number);
  const pb = b.split('.').map(Number);
  for (let i = 0; i < 3; i++) {
    const diff = (pa[i] ?? 0) - (pb[i] ?? 0);
    if (diff !== 0) return diff;
  }
  return 0;
}
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && npx tsc --noEmit --project packages/cli/tsconfig.json 2>&1 | head -20</automated></verify>
<done>Installer mostra versao do bun e avisa se < 1.1.0. Nao bloqueia instalacao (apenas warning).</done>
</task>

<task id="7" type="auto">
<files>
packages/core/src/memory-manager.ts
packages/core/src/context-builder.ts
packages/core/src/memory/janitor.ts
packages/core/src/memory/writer.ts
packages/dashboard/src/lib/core.ts
packages/core/src/claude-runner.ts
packages/cli/src/commands/install.ts
</files>
<action>
Verificacao final: rodar grep em TODO o codebase para confirmar que nao restou nenhuma referencia hardcoded a '/home/vault/05-pessoal/daily-log'.

Executar:
```bash
grep -rn '/home/vault/05-pessoal/daily-log' packages/
```

Se encontrar algum resultado, corrigir o arquivo aplicando o mesmo padrao de resolucao (env > config.vaultPath > default).

Tambem verificar que o build completo passa:
```bash
cd /home/projects/ForgeClaw && bun run build 2>&1 | tail -20
```

E rodar os testes existentes:
```bash
cd /home/projects/ForgeClaw && bun test 2>&1 | tail -30
```
</action>
<verify><automated>cd /home/projects/ForgeClaw && grep -rn '/home/vault/05-pessoal/daily-log' packages/ | grep -v node_modules | grep -v '.plano/' ; echo "EXIT: $?"</automated></verify>
<done>Zero ocorrencias de '/home/vault/05-pessoal/daily-log' no codebase. Build e testes passam.</done>
</task>

## Criterios de Sucesso

- [ ] Nenhum arquivo contem '/home/vault/05-pessoal/daily-log' hardcoded
- [ ] ForgeClawConfig tem skipPermissions?: boolean
- [ ] claude-runner.ts le config.skipPermissions e so adiciona flag se !== false
- [ ] Installer mostra versao do bun e avisa se < 1.1.0
- [ ] TypeScript compila sem erros em core, bot, dashboard, cli
