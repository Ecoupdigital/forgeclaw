---
phase: 21-mission-control
plan: 21-02
type: feature
autonomous: true
wave: 1
depends_on: [21-01]
requirements: [MC-02, MC-06]
must_haves:
  truths:
    - "Cada stream:done no ws-server.ts e text.ts persiste uma row em token_usage com input_tokens, output_tokens, cache_creation_tokens, cache_read_tokens"
    - "Eventos do sistema (session, cron, message) geram rows em activities via activity-recorder"
    - "activity:created e emitido no EventBus apos cada activity ser gravada"
  artifacts:
    - path: "packages/core/src/token-recorder.ts"
      provides: "Modulo que escuta stream:done e grava token_usage"
    - path: "packages/core/src/activity-recorder.ts"
      provides: "Modulo que escuta eventos do EventBus e grava activities + emite activity:created"
    - path: "packages/core/src/index.ts"
      provides: "Re-exports dos novos modulos"
  key_links:
    - from: "token-recorder.ts"
      to: "event-bus.ts"
      via: "eventBus.on('stream:done', handler)"
    - from: "activity-recorder.ts"
      to: "event-bus.ts"
      via: "eventBus.on('session:created' | 'cron:result' | 'message:incoming' | 'message:outgoing', handler)"
    - from: "activity-recorder.ts"
      to: "state-store.ts"
      via: "stateStore.createActivity()"
---

# Fase 21 Plano 02: Token Recorder & Activity Recorder

**Objetivo:** Criar os dois modulos core que capturam dados de uso de tokens e atividades do sistema, escutando eventos do EventBus e persistindo no SQLite.

## Contexto

@packages/core/src/event-bus.ts -- EventBus com eventos tipados, agora inclui 'activity:created'
@packages/core/src/state-store.ts -- StateStore com metodos createTokenUsage e createActivity (do plano 21-01)
@packages/core/src/ws-server.ts -- onde stream:done e capturado para mensagens do dashboard (linhas 225-236)
@packages/bot/src/handlers/text.ts -- onde stream:done e capturado para mensagens do Telegram (linhas 219-234)
@packages/core/src/cron-engine.ts -- onde cron:fired e cron:result sao emitidos
@packages/core/src/types.ts -- TokenUsage, Activity, ActivityType interfaces

## Tarefas

<task id="1" type="auto">
<files>packages/core/src/token-recorder.ts</files>
<action>
Criar novo arquivo `packages/core/src/token-recorder.ts`. Este modulo escuta o evento `stream:done` do EventBus e persiste os dados de uso de tokens na tabela `token_usage`.

```typescript
import { eventBus } from './event-bus';
import { stateStore } from './state-store';

/**
 * Token Recorder — listens to stream:done events and persists token usage.
 *
 * Usage data comes from the Claude CLI stream in two shapes:
 * 1. Standard: { input_tokens, output_tokens }
 * 2. Extended (cache): { input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens }
 *
 * Both ws-server.ts and bot/text.ts already extract `event.data.usage` on
 * stream:done. This module centralizes persistence so neither caller needs
 * to write to the DB directly.
 *
 * Call `startTokenRecorder()` once at boot (in bot/index.ts and ws-server.ts).
 */

let started = false;

export function startTokenRecorder(): void {
  if (started) return;
  started = true;

  eventBus.on('stream:done', async (data) => {
    try {
      const sessionKey = data.sessionKey as string | undefined;
      if (!sessionKey) return;

      const usage = data.usage as {
        input_tokens?: number;
        output_tokens?: number;
        cache_creation_input_tokens?: number;
        cache_read_input_tokens?: number;
      } | undefined;

      if (!usage) return;

      const inputTokens = usage.input_tokens ?? 0;
      const outputTokens = usage.output_tokens ?? 0;
      // If both are zero, nothing to record
      if (inputTokens === 0 && outputTokens === 0) return;

      const topicId = data.topicId as number | null ?? null;
      const model = data.model as string | null ?? null;
      const source = (data.source as string | undefined) ?? 'telegram';

      stateStore.createTokenUsage({
        sessionKey,
        topicId,
        inputTokens,
        outputTokens,
        cacheCreationTokens: usage.cache_creation_input_tokens ?? 0,
        cacheReadTokens: usage.cache_read_input_tokens ?? 0,
        model,
        source: source as 'dashboard' | 'telegram' | 'cron',
        createdAt: Date.now(),
      });
    } catch (err) {
      console.error('[token-recorder] Failed to record token usage:', err);
    }
  });

  console.log('[token-recorder] Started — listening for stream:done events');
}
```

Este modulo NAO modifica ws-server.ts ou text.ts. Ele escuta o evento via EventBus.
</action>
<verify>
<automated>cd /home/projects/ForgeClaw && bunx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | head -20</automated>
</verify>
<done>token-recorder.ts escuta stream:done e persiste em token_usage</done>
</task>

<task id="2" type="auto">
<files>packages/core/src/ws-server.ts, packages/bot/src/handlers/text.ts</files>
<action>
Modificar os dois locais onde `stream:done` e processado para incluir dados extras no emit do EventBus que o token-recorder precisa.

**Em `packages/core/src/ws-server.ts`**, no bloco `if (event.type === 'done')` (por volta da linha 225), APOS a linha que faz `broadcastToSubscribers`, adicionar um emit de `stream:done` no EventBus com os dados de usage. Localizar onde `usage` e extraido (linha 231) e APOS o bloco de context usage, adicionar:

```typescript
// Emit stream:done for token-recorder (Mission Control)
eventBus.emit('stream:done', {
  sessionKey,
  topicId,
  usage: usage ?? undefined,
  model: runOptions.model as string | undefined ?? null,
  source: 'dashboard',
});
```

Verificar que `eventBus` ja esta importado no topo do arquivo (linha 7: `import { eventBus } from './event-bus';`) -- ja esta.

**Em `packages/bot/src/handlers/text.ts`**, dentro do case `'done':` (por volta da linha 219), APOS a linha `await sessionManager.updateContextUsage(...)` (linha 232) e ANTES do build da mensagem final, adicionar:

```typescript
// Emit stream:done for token-recorder (Mission Control)
eventBus.emit('stream:done', {
  sessionKey,
  topicId,
  usage: usage ?? undefined,
  model: config.claudeModel ?? null,
  source: 'telegram',
});
```

Verificar que `eventBus` ja esta importado em text.ts (linha 13: `eventBus` no import de @forgeclaw/core) -- ja esta.

NOTA CRITICA: O `usage` extraido atualmente so tem `input_tokens` e `output_tokens`. Os campos de cache (`cache_creation_input_tokens`, `cache_read_input_tokens`) vem no mesmo objeto `event.data.usage` do stream NDJSON mas NAO sao tipados no cast atual. O token-recorder ja lida com campos opcionais via `?? 0`, entao nao e preciso mudar o cast — os campos extras fluem como unknown properties do JS object.

Entretanto, para capturar os cache tokens corretamente, modificar o cast de `usage` em AMBOS os arquivos para ser mais abrangente:

**ws-server.ts (linha 231):** Mudar de:
```typescript
const usage = event.data.usage as { input_tokens: number; output_tokens: number } | undefined;
```
Para:
```typescript
const usage = event.data.usage as {
  input_tokens: number;
  output_tokens: number;
  cache_creation_input_tokens?: number;
  cache_read_input_tokens?: number;
} | undefined;
```

**text.ts (linha 228):** Mudar de:
```typescript
const usage = event.data.usage as { input_tokens: number; output_tokens: number } | undefined;
```
Para:
```typescript
const usage = event.data.usage as {
  input_tokens: number;
  output_tokens: number;
  cache_creation_input_tokens?: number;
  cache_read_input_tokens?: number;
} | undefined;
```
</action>
<verify>
<automated>cd /home/projects/ForgeClaw && bunx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | head -20 && cd /home/projects/ForgeClaw && bunx tsc --noEmit --project packages/bot/tsconfig.json 2>&1 | head -20</automated>
</verify>
<done>ws-server.ts e text.ts emitem stream:done no EventBus com sessionKey, topicId, usage (incluindo cache tokens), model, source</done>
</task>

<task id="3" type="auto">
<files>packages/core/src/activity-recorder.ts</files>
<action>
Criar novo arquivo `packages/core/src/activity-recorder.ts`. Este modulo escuta varios eventos do EventBus e cria registros na tabela `activities`, depois emite `activity:created` para que o webhook dispatcher (plano 21-03) possa reagir.

```typescript
import { eventBus } from './event-bus';
import { stateStore } from './state-store';
import type { ActivityType, ActivityEntityType } from './types';

/**
 * Activity Recorder — creates an audit trail of system events.
 *
 * Listens to EventBus events and writes rows to the `activities` table.
 * After each write, emits 'activity:created' so downstream consumers
 * (webhook dispatcher, real-time dashboard feed) can react.
 *
 * Call `startActivityRecorder()` once at boot.
 */

let started = false;

async function record(
  type: ActivityType,
  entityType: ActivityEntityType,
  entityId: string,
  actor: string,
  description: string,
  metadata?: Record<string, unknown>,
): Promise<void> {
  try {
    const id = stateStore.createActivity({
      type,
      entityType,
      entityId,
      actor,
      description,
      metadata: metadata ?? null,
      createdAt: Date.now(),
    });

    // Notify downstream (webhook dispatcher, dashboard feed)
    await eventBus.emit('activity:created', {
      activityId: id,
      type,
      entityType,
      entityId,
      actor,
      description,
      metadata: metadata ?? null,
    });
  } catch (err) {
    console.error(`[activity-recorder] Failed to record ${type}:`, err);
  }
}

export function startActivityRecorder(): void {
  if (started) return;
  started = true;

  // Session events
  eventBus.on('session:created', async (data) => {
    const sessionKey = data.sessionKey as string ?? 'unknown';
    const topicId = data.topicId as number | null;
    await record(
      'session:created',
      'session',
      sessionKey,
      'system',
      `Nova sessao criada para ${sessionKey}`,
      { topicId },
    );
  });

  eventBus.on('session:resumed', async (data) => {
    const sessionKey = data.sessionKey as string ?? 'unknown';
    const topicId = data.topicId as number | null;
    await record(
      'session:resumed',
      'session',
      sessionKey,
      'system',
      `Sessao retomada: ${sessionKey}`,
      { topicId },
    );
  });

  // Message events
  eventBus.on('message:incoming', async (data) => {
    const sessionKey = data.sessionKey as string ?? 'unknown';
    const origin = data.origin as string ?? 'unknown';
    const content = data.content as string ?? '';
    await record(
      'message:sent',
      'message',
      sessionKey,
      origin,
      `Mensagem enviada (${content.length} chars)`,
      { origin, contentPreview: content.slice(0, 100) },
    );
  });

  eventBus.on('message:outgoing', async (data) => {
    const sessionKey = data.sessionKey as string ?? 'unknown';
    const origin = data.origin as string ?? 'unknown';
    const content = data.content as string ?? '';
    await record(
      'message:received',
      'message',
      sessionKey,
      'claude',
      `Resposta do Claude (${content.length} chars)`,
      { origin, contentPreview: content.slice(0, 100) },
    );
  });

  // Cron events
  eventBus.on('cron:fired', async (data) => {
    const jobId = data.jobId as number;
    const name = data.name as string ?? 'unknown';
    await record(
      'cron:fired',
      'cron',
      String(jobId),
      'system',
      `Cron disparado: ${name}`,
      { jobId, name },
    );
  });

  eventBus.on('cron:result', async (data) => {
    const jobId = data.jobId as number;
    const jobName = data.jobName as string ?? 'unknown';
    const status = data.status as string;
    const activityType: ActivityType = status === 'success' ? 'cron:completed' : 'cron:failed';
    await record(
      activityType,
      'cron',
      String(jobId),
      'system',
      `Cron ${status}: ${jobName}`,
      { jobId, jobName, status },
    );
  });

  console.log('[activity-recorder] Started — listening for system events');
}
```
</action>
<verify>
<automated>cd /home/projects/ForgeClaw && bunx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | head -20</automated>
</verify>
<done>activity-recorder.ts escuta session:created, session:resumed, message:incoming, message:outgoing, cron:fired, cron:result e grava activities + emite activity:created</done>
</task>

<task id="4" type="auto">
<files>packages/core/src/index.ts</files>
<action>
Adicionar ao final de `packages/core/src/index.ts` os re-exports dos novos modulos:

```typescript
export * from './token-recorder';
export * from './activity-recorder';
```

Adicionar APOS a linha `export * from './ws-server';` (atualmente a penultima linha).
</action>
<verify>
<automated>cd /home/projects/ForgeClaw && bunx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | head -10</automated>
</verify>
<done>packages/core exporta startTokenRecorder e startActivityRecorder</done>
</task>

<task id="5" type="auto">
<files>packages/bot/src/index.ts</files>
<action>
No arquivo de bootstrap do bot (`packages/bot/src/index.ts`), importar e inicializar os recorders. Localizar o bloco de startup (onde `bot.start()` ou similar e chamado) e adicionar ANTES do start:

```typescript
import { startTokenRecorder, startActivityRecorder } from '@forgeclaw/core';
```

E no corpo de inicializacao (proximo de onde cronEngine.start() e chamado), adicionar:

```typescript
// Mission Control: start token & activity recorders
startTokenRecorder();
startActivityRecorder();
```

Se o bot ja importa coisas de `@forgeclaw/core`, adicionar `startTokenRecorder, startActivityRecorder` ao import existente em vez de criar um novo.

Verificar o arquivo primeiro para identificar o local exato. Grep por `cronEngine.start` ou `bot.start` para encontrar o ponto de bootstrap.
</action>
<verify>
<automated>cd /home/projects/ForgeClaw && grep -n 'startTokenRecorder\|startActivityRecorder' packages/bot/src/index.ts</automated>
</verify>
<done>Bot inicializa token-recorder e activity-recorder no startup</done>
</task>

## Criterios de Sucesso

- [ ] Cada stream:done persiste input_tokens, output_tokens, cache_creation_tokens, cache_read_tokens na tabela token_usage
- [ ] Cache tokens (cache_creation_input_tokens, cache_read_input_tokens) sao capturados do stream data
- [ ] Eventos de sessao, mensagem e cron geram rows na tabela activities
- [ ] activity:created e emitido apos cada gravacao de activity
- [ ] `bunx tsc --noEmit` passa em core e bot sem erros
